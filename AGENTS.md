# Agent 工作原則（本 repo 專屬）

適用 Claude Code、Codex、OpenCode 等所有 AI agent，不論以哪個目錄為 workspace root 開 session。

- 工作紀錄與長期 context 一律記到 `D:\力山\.agents\`：
  - 當日紀錄：`D:\力山\.agents\work-logs\專利_ppt自動\{日期}.md`（三 agent 共用同一檔，依線分節）。
  - 穩定背景：`D:\力山\.agents\context\`（路由見該目錄 `README.md`）。
- 本 repo 內**不建立** `.agents/` 目錄；若發現殘留，先併回中央再移除。
- 全域規則唯一來源：`D:\力山\.ai-rules\`（shared.md 為主）。

## 精準 TDD（本專案試行，自 2026-07-21）

- Red → 最小 Green → **必要時才** Refactor；不是每輪都硬做第三步。
- Red 必須真實執行並如實記錄失敗原因；不得先改檔後補造 Red。
- 錯誤修正先建 regression test；migration 先建契約測試。
- 每個增量先設停止點，達成即停止回報，等驗收再進下一段。

## 提問與任務拆分（2026-07-31 定案）

### 事實自己查，決策才問人

- 能從環境查到的**事實**（檔案內容、行程狀態、設定值、實際輸出、log）一律自己查，
  不得拿來問使用者。⚠ 問一個查得到的事實，等於把成本轉嫁給使用者，
  而且回答還可能是記憶而非現況。
- **決策**（要不要做、做哪一個、界線在哪、優先序）一律交給使用者，不得替他吸收。
- 分不清是事實還是決策時：先試著查；查得到就是事實，查不到才是決策。

### 一次問一題

- 提問**一次一題**，等回答再問下一題。一次丟多題會讓人無法逐題思考。
- 每一題都要附上**你建議的答案**與理由；使用者只需要說「就這樣」即可推進。
- ⚠ 例外：彼此**完全獨立**且無相依的並列選擇（例如四個互不影響的外觀參數），
  可用選項表單一次問完。只要有「A 的答案會改變 B 的選項」的相依關係，就必須拆開逐題問。

### 任務依**需求功能**拆，不依技術層拆

- 拆任務的單位是**使用者看得到的一個能力**（一條端到端的路徑），
  不是「先做 DB、再做 API、最後做前端」的水平切法。
- ⚠ 依技術層拆會讓每一層都無法單獨驗收——DB 做完看不出對錯，要等前端接上才知道，
  問題累積到最後一起爆。依功能拆則每一段完成即可端到端驗，這也是 TDD 能運作的前提
  （測試要能表達「使用者要的行為」，而不是「這層函式回傳什麼」）。
- 一個功能切片跨越多層是正常的；跨層不是拆分的理由，**無法獨立驗收**才是。

## 驗收產物落點（2026-07-31 定案）

- 驗收時產的 PPT、轉圖、截圖一律放 `output/_verify/<主題>/`（`output/` 已在 `.gitignore`）。
- ⚠ 不要放 C 槽暫存區：跨 session 找不回來，也讓「這次驗的是哪一版」無從追溯。
  C 槽只留與專案無關、用完即丟的腳本。
- ⚠ 轉圖前**先清空輸出目錄**：PowerPoint COM 匯出遇到同名檔會留下上一輪的圖，
  會讓人誤判「修正沒生效」（2026-07-31 實際發生過）。
- 🔴 **只保留最新一份**：產新的驗收產物前，先清掉 `output/_verify/` 底下的舊目錄。
  ⚠ 2026-08-02 實際累積到 **27 個目錄／90.9 MB**——每輪開一個新名字（b2／b2r2／
  b3／final…）卻從不清理。驗收產物的用途是「這輪改完長怎樣」，上一輪留著沒有價值；
  要回溯有 git 與 commit 訊息。
- ⚠ 換落點不等於解決問題：把暫存從 C 槽搬到專案 `output/` 只改了位置，
  沒設保留策略就只是換個地方繼續膨脹。

## 🔴 驗收基準：轉圖 ≠ 實機（2026-08-02 實證）

`pptx_to_png.py`（PowerPoint COM）轉出的圖與使用者**開檔實際看到的**不一樣。

實證：同一份 pptx，p20「態勢」卡的斷行位置不同——

| | 斷行 |
|---|---|
| COM 轉圖 | 申請2022年15件高峰、2024年11件次 / **高** ← 末字單獨掉行 |
| 實機開檔 | 申請2022年15件高峰、2024年 / 11件次高 ← 正常 |

實機每行字數較少、字約大一成。⚠ 這推翻了「轉圖輸出即使用者開檔會看到的樣子」。

**轉圖驗得準的**：文字內容錯誤、顏色、元素有無、圖表資料、欄位外洩。
**轉圖驗不準的**：字級、斷行、空白率、行數、任何靠像素量的比例。
**轉圖完全看不到的**：拼字檢查的紅色波浪線（proofing marks 不進圖）。

⚠ 靠轉圖像素量出來的結論（空白率、字太小、末字掉行）**一律要用實機截圖複驗**
再寫進待辦——2026-08-02 的 F-14「末字掉行」就是轉圖假象，實機並無此問題。

### SVG 文字的最終字級下限

引擎畫的 SVG 文字寫死 px，圖再被縮進 PPT 圖框 → **縮了兩次**。
組版原生文字（卡片、面板）由 theme 控制字級、不受二次縮放影響，兩者因此落差很大。

判準是**縮放後的實際字級**，不是 SVG 裡寫的數字。
⚠ 所以「把 SVG 字級調大」不一定有效——圖被縮得更多時等於沒改。

## 驗收要一併審視結構（2026-07-31 定案）

程式驗收不只看「行為對不對」，要同時審視結構是否健康：

- **同一資訊是否有兩處落點**：同一份規則、對照表、常數是否被抄成兩份各自演進。
  ⚠ 本專案已重複發生四次（前端／後端欄名、report_keys、workspace_id、表格欄位對照），
  每次症狀都是「欄位在、位置不對」或「兩邊各自漂移」，且都驗不出來。
- **跨行程／跨容器的資料是否走同一條路**：檔案系統與 DB、worker 與 backend
  是否讀同一份來源。
- **規則寫在提示裡還是寫成檢查**：只寫在給 AI 看的提示、沒有程式驗證的規則，
  等於沒有規則（見 `known-issues-optimization.md` C-1）。
- **新增的抽象是否降低了實際複雜度**，還是只是多一層。

### 模組是深結構還是碎塊

用詞固定（避免「元件／服務／邊界」這類各說各話的字）：

- **介面**＝呼叫端為了正確使用它**必須知道的全部**——不只型別簽名，還包括前後順序限制、
  錯誤情形、必要設定、效能特性。
- **深模組**＝介面小、實作厚（大量行為藏在少數入口後面）。
  **碎塊（淺模組）**＝介面幾乎和實作一樣複雜，等於只是轉手。

三個可以當場問的判準：

- **刪除測試**：想像把這個模組刪掉。複雜度就此消失＝它只是轉手，該刪；
  複雜度在 N 個呼叫端各自重現＝它有在做事，該留。
- **介面就是測試面**：如果測試必須繞過介面才驗得到，代表模組的形狀不對。
- **一個 adapter 只是假想的接縫，兩個才是真的**：沒有東西真的在變，就不要為了「日後可換」
  先開接縫。

### 入口要精簡、要統一

- 新增能力時先問：**能不能不加參數**？加參數是把成本轉嫁給每一個呼叫端與每一個測試。
- 同一件事若有兩個以上入口（兩個函式、兩份對照表、兩條讀取路徑），視為結構缺陷，
  除非能說出兩者**語意確實不同**。

⚠ 本專案的實例（2026-07-31，兩種做法對照）：

| 做法 | 結果 |
|---|---|
| `rasterize_svg(svg, cache_dir)` 加第三個參數 `theme` | 介面變寬，所有呼叫端與測試都得跟著改（實際造成一支測試紅） |
| `_add_picture_fitted` 改為「**同名 `.svg` 存在就一併掛向量**」 | 介面完全不變，呼叫端一處都不用改——同一份需求，深化的寫法 |

兩者是同一天、同一個功能的兩種選擇；差別只在**有沒有把複雜度藏進實作**。

發現結構問題時據實列出，不得因為「測試過了」就跳過。

（深模組／接縫／刪除測試等用語取自 Ousterhout《A Philosophy of Software Design》
與 Feathers 的 seam 概念；整理參考 `mattpocock/skills` 的 `codebase-design`。）

## Token 節制（本專案試行，自 2026-07-21）

- 小型**已定位**任務以約 3,000 output tokens 為軟目標；migration、跨表、資料安全相關工作以**安全完成優先，不設硬上限**。
- 只削減四樣：全庫掃描、重複讀檔、長 traceback、冗長回報。
- **不得削減**：必要契約測試、資料搬移驗證、downgrade 驗證。
- 同一失敗最多修兩輪，仍失敗即停止並回報阻塞。

（試行條款：先只作本專案層級規則；跑一段時間驗收後再決定是否升格 `D:\力山\.ai-rules\`。）

## Skill 撰寫硬規範（2026-07-21 起，所有 agent 一體適用）

- 本專案所有 skill **最終都會佈署到使用者電腦**，必須讓「零專案背景的 CLI」看了就能執行——這是使用者反覆重申的既有要求，不是建議。
- 每份 skill 固定兩區：**「執行 Runbook」在前**（觸發時機、逐步驟、呼叫的 MCP 工具名、輸入輸出契約、閘門呈現方式；不得出現開發機路徑、直連 DB 指令、migration 編號等開發上下文；依賴工具尚未存在時明確標「待工具：<名>」）；**「開發備註」在後**（設計原因、決策沿革、環境細節）。
- 新增或修改 skill 未照此結構＝驗收不過。Installer 打包＝機械抽取 Runbook 區，不得需要人工改寫。
