# Agent 工作原則（本 repo 專屬）

適用 Claude Code、Codex、OpenCode 等所有 AI agent，不論以哪個目錄為 workspace root 開 session。

- 工作紀錄與長期 context 一律記到 `D:\力山\.agents\`：
  - 當日紀錄：`D:\力山\.agents\work-logs\專利_ppt自動\{日期}.md`（三 agent 共用同一檔，依線分節）。
  - 穩定背景：`D:\力山\.agents\context\`（路由見該目錄 `README.md`）。
- 本 repo 內**不建立** `.agents/` 目錄；若發現殘留，先併回中央再移除。
- 全域規則唯一來源：`D:\力山\.ai-rules\`；開始工作前必須讀取 `shared.md` 與 `workflows.md`，不得只讀本檔。

## OpenSpec 規格管理

- 功能規格唯一來源為本 repo 的 `openspec/`：
  - `openspec/specs/`：已實作、已驗收且目前有效的能力。
  - `openspec/changes/`：規劃中、實作中或尚未完成驗收的變更。
  - `openspec/changes/archive/`：已驗收變更的歷史與決策軌跡。
- `docs/` 只保存架構參考、操作 Runbook 與外部參考；不得再新增功能規格正文。
- `D:\力山\.agents\context\` 只保存背景、狀態與正式決策；若提及需求，只交叉引用 OpenSpec，不複製規格。
- OpenSpec 的 `validate` 只驗規格結構，不能取代本檔的精準 TDD、模組驗證、DB smoke 或實物驗收。
- 只有使用者明確確認驗收通過，才可執行 OpenSpec archive。
- OpenSpec 產生的 Codex skills 一律放中央 `D:\力山\.agents\skills\`；本 repo 仍不得建立 `.agents/`。

### 需求、分支與交付閘門

- 所有 agent 依相同順序工作：需求釐清 → 遠端工作分支 → OpenSpec 規劃與規格確認 → TDD → 組合驗收 → 使用者接受 → archive → PR checks → 合併主線。
- 可由程式、DB、設定、log 或輸出查到的事實自行查；會影響 scope、行為、資料、權限、輸出或驗收的決策依相依順序一次問一題。阻塞問題未清空前不得寫規格。
- proposal 必須明列已確認決策、未決問題與 Acceptance Gate；使用者確認完整 artifacts 前不得執行 apply。
- 第一次改檔前必須從最新 `master` 建立 `<type>/<change-id>` 分支並先推到 `origin`。不得直接在 `master` 修改、提交、推送或繞過 PR；原則上一個 OpenSpec change 對應一個 branch／PR。
- 組合驗收至少包含 OpenSpec strict validation、目標測試、範圍回歸、結構檢查，以及適用的 DB／API／前端／CLI／PPT 實物驗收。未適用與未執行必須分開列出。
- 只有使用者明確接受組合驗收才可 archive；archive 後提交並推送同一分支，required checks 全綠且使用者允許後才可合併 `master`。
- Claude Code 由 `CLAUDE.md` 匯入本檔；Codex 直接讀本檔；OpenCode 由 repo `opencode.json` 匯入本檔。工具專屬 OpenSpec commands／skills 不得覆蓋上述閘門。

## 精準 TDD（本專案試行，自 2026-07-21）

- Red → 最小 Green → **必要時才** Refactor；不是每輪都硬做第三步。
- Red 必須真實執行並如實記錄失敗原因；不得先改檔後補造 Red。
- 錯誤修正先建 regression test；migration 先建契約測試。
- 每個增量先設停止點，達成即停止回報，等驗收再進下一段。

## 提問與任務拆分（2026-07-31 定案）

### 事實自己查，決策才問人

- 能從環境查到的**事實**（檔案內容、行程狀態、設定值、實際輸出、log）一律自己查，
  不得拿來問使用者。⚠ 問一個查得到的事實，等於把成本轉嫁給使用者，
  而且回答還可能是記憶而非現況。
- **決策**（要不要做、做哪一個、界線在哪、優先序）一律交給使用者，不得替他吸收。
- 分不清是事實還是決策時：先試著查；查得到就是事實，查不到才是決策。

### 一次問一題

- 提問**一次一題**，等回答再問下一題。一次丟多題會讓人無法逐題思考。
- 每一題都要附上**你建議的答案**與理由；使用者只需要說「就這樣」即可推進。
- ⚠ 例外：彼此**完全獨立**且無相依的並列選擇（例如四個互不影響的外觀參數），
  可用選項表單一次問完。只要有「A 的答案會改變 B 的選項」的相依關係，就必須拆開逐題問。

### 任務依**需求功能**拆，不依技術層拆

- 拆任務的單位是**使用者看得到的一個能力**（一條端到端的路徑），
  不是「先做 DB、再做 API、最後做前端」的水平切法。
- ⚠ 依技術層拆會讓每一層都無法單獨驗收——DB 做完看不出對錯，要等前端接上才知道，
  問題累積到最後一起爆。依功能拆則每一段完成即可端到端驗，這也是 TDD 能運作的前提
  （測試要能表達「使用者要的行為」，而不是「這層函式回傳什麼」）。
- 一個功能切片跨越多層是正常的；跨層不是拆分的理由，**無法獨立驗收**才是。

## 驗收產物落點（2026-07-31 定案）

- 驗收時產的 PPT、轉圖、截圖一律放 `output/_verify/<主題>/`（`output/` 已在 `.gitignore`）。
- ⚠ 不要放 C 槽暫存區：跨 session 找不回來，也讓「這次驗的是哪一版」無從追溯。
  C 槽只留與專案無關、用完即丟的腳本。
- ⚠ 轉圖前**先清空輸出目錄**：PowerPoint COM 匯出遇到同名檔會留下上一輪的圖，
  會讓人誤判「修正沒生效」（2026-07-31 實際發生過）。
- 🔴 **只保留最新一份**：產新的驗收產物前，先清掉 `output/_verify/` 底下的舊目錄。
  ⚠ 2026-08-02 實際累積到 **27 個目錄／90.9 MB**——每輪開一個新名字（b2／b2r2／
  b3／final…）卻從不清理。驗收產物的用途是「這輪改完長怎樣」，上一輪留著沒有價值；
  要回溯有 git 與 commit 訊息。
- ⚠ 換落點不等於解決問題：把暫存從 C 槽搬到專案 `output/` 只改了位置，
  沒設保留策略就只是換個地方繼續膨脹。

## 🔴 驗收基準：轉圖 ≠ 實機（2026-08-02 實證）

`pptx_to_png.py`（PowerPoint COM）轉出的圖與使用者**開檔實際看到的**不一樣。

實證：同一份 pptx，p20「態勢」卡的斷行位置不同——

| | 斷行 |
|---|---|
| COM 轉圖 | 申請2022年15件高峰、2024年11件次 / **高** ← 末字單獨掉行 |
| 實機開檔 | 申請2022年15件高峰、2024年 / 11件次高 ← 正常 |

實機每行字數較少、字約大一成。⚠ 這推翻了「轉圖輸出即使用者開檔會看到的樣子」。

**轉圖驗得準的**：文字內容錯誤、顏色、元素有無、圖表資料、欄位外洩。
**轉圖驗不準的**：字級、斷行、空白率、行數、任何靠像素量的比例。
**轉圖完全看不到的**：拼字檢查的紅色波浪線（proofing marks 不進圖）。

⚠ 靠轉圖像素量出來的結論（空白率、字太小、末字掉行）**一律要用實機截圖複驗**
再寫進待辦——2026-08-02 的 F-14「末字掉行」就是轉圖假象，實機並無此問題。

### SVG 文字的最終字級下限

引擎畫的 SVG 文字寫死 px，圖再被縮進 PPT 圖框 → **縮了兩次**。
組版原生文字（卡片、面板）由 theme 控制字級、不受二次縮放影響，兩者因此落差很大。

判準是**縮放後的實際字級**，不是 SVG 裡寫的數字。
⚠ 所以「把 SVG 字級調大」不一定有效——圖被縮得更多時等於沒改。

## 驗收要一併審視結構（2026-07-31 定案）

程式驗收不只看「行為對不對」，要同時審視結構是否健康：

- **同一資訊是否有兩處落點**：同一份規則、對照表、常數是否被抄成兩份各自演進。
  ⚠ 本專案已重複發生四次（前端／後端欄名、report_keys、workspace_id、表格欄位對照），
  每次症狀都是「欄位在、位置不對」或「兩邊各自漂移」，且都驗不出來。
- **跨行程／跨容器的資料是否走同一條路**：檔案系統與 DB、worker 與 backend
  是否讀同一份來源。
- **規則寫在提示裡還是寫成檢查**：只寫在給 AI 看的提示、沒有程式驗證的規則，
  等於沒有規則（見 `known-issues-optimization.md` C-1）。
- **新增的抽象是否降低了實際複雜度**，還是只是多一層。

### 模組是深結構還是碎塊

用詞固定（避免「元件／服務／邊界」這類各說各話的字）：

- **介面**＝呼叫端為了正確使用它**必須知道的全部**——不只型別簽名，還包括前後順序限制、
  錯誤情形、必要設定、效能特性。
- **深模組**＝介面小、實作厚（大量行為藏在少數入口後面）。
  **碎塊（淺模組）**＝介面幾乎和實作一樣複雜，等於只是轉手。

三個可以當場問的判準：

- **刪除測試**：想像把這個模組刪掉。複雜度就此消失＝它只是轉手，該刪；
  複雜度在 N 個呼叫端各自重現＝它有在做事，該留。
- **介面就是測試面**：如果測試必須繞過介面才驗得到，代表模組的形狀不對。
- **一個 adapter 只是假想的接縫，兩個才是真的**：沒有東西真的在變，就不要為了「日後可換」
  先開接縫。

### 入口要精簡、要統一

- 新增能力時先問：**能不能不加參數**？加參數是把成本轉嫁給每一個呼叫端與每一個測試。
- 同一件事若有兩個以上入口（兩個函式、兩份對照表、兩條讀取路徑），視為結構缺陷，
  除非能說出兩者**語意確實不同**。

⚠ 本專案的實例（2026-07-31，兩種做法對照）：

| 做法 | 結果 |
|---|---|
| `rasterize_svg(svg, cache_dir)` 加第三個參數 `theme` | 介面變寬，所有呼叫端與測試都得跟著改（實際造成一支測試紅） |
| `_add_picture_fitted` 改為「**同名 `.svg` 存在就一併掛向量**」 | 介面完全不變，呼叫端一處都不用改——同一份需求，深化的寫法 |

兩者是同一天、同一個功能的兩種選擇；差別只在**有沒有把複雜度藏進實作**。

發現結構問題時據實列出，不得因為「測試過了」就跳過。

（深模組／接縫／刪除測試等用語取自 Ousterhout《A Philosophy of Software Design》
與 Feathers 的 seam 概念；整理參考 `mattpocock/skills` 的 `codebase-design`。）

## 回歸只跑範圍（2026-08-03 使用者定案）

**預設跑範圍回歸，不跑完整回歸。**

```
uv run python -m pytest tests -q -p no:cacheprovider -k "<本次改動相關的關鍵字>" --ignore=tests/test_clustering_database.py --ignore=tests/test_report_artifact_store.py --ignore=tests/test_api_report_versions.py --ignore=tests/test_api_exclude_patents.py --ignore=tests/test_api_exclusion_reviews.py --deselect tests/test_per_channel_topic_labels.py
```

⚠ 那 13 支**全部依賴本機 postgres**（2026-08-03 由獨立 sub agent 兩輪完整回歸查證）。
本機沒起 DB 時每支空等 **30 秒 pool timeout**，是總時長的主因
——實測排除後由 **40 分鐘降到 6 分鐘**，同一批 893 個測試。

🔴 **不要為了跑測試去起本機 postgres**（使用者明示「不要亂起」）。排除即可。

⚠ **回歸跑到一半不要改檔**：pytest 在 collection 階段匯入模組，
中途改檔會讓它拿到改到一半的版本，紅出來的**不是真迴歸**
（2026-08-03 發生兩次：一次害 `test_default_report_names_match_definitions` 假紅、
一次害 `test_signature_returns_pair` 與 `test_appendix_uses_even_split` 假紅）。
範圍回歸只要 6 分鐘，暴露窗口本來就小——真要跑長的就先 commit 再跑。

## 🔴 報表增修的優先序：刪 > 改版 > 新增（2026-08-05 使用者定案）

要解一個報表／簡報問題時，**依序**考慮：

1. **能不能刪**——這張報表是否本來就沒在回答問題、或與另一張重疊？先刪掉。
2. **能不能拿某張現有報表改版**——同一份資料換個軸、換個口徑、加一欄，
   往往就是要的東西。
3. **真的都不行，才新增。**

⚠ 為什麼是這個順序：報表不是越多越專業。每多一張就多一份要維護的版面、
容量、字級、解讀契約與測試——本專案已有 15 張報表、22 頁簡報，
再往上加，讀者的注意力與我們的維護成本都會先撐爆。
一份「該講的都講到、沒有一頁是廢話」的報告，比一份「什麼都有」的報告專業。

⚠ 提案時要**明講走的是哪一條**。寫「新增 X 報表」而沒有先交代
「為什麼不能刪、不能改版」，視為規劃未完成。

⚠ 刪除要留痕：刪掉的報表要記在 `.agents/context/` 的對應主題檔，
寫明**為何刪**與**它原本要回答的問題現在由誰回答**——否則下次有人會再加回來。

## 模組驗證流程（本專案試行，自 2026-08-05）

**觸發時機：一個模組／功能做完，交付驗收之前。** 不是每次改一行都跑；
是「這塊做完了」的那一刻跑完整套，跑完才算做完。

⚠ 本流程與「精準 TDD」不重複：TDD 管的是**開發當下**（Red→Green），
本流程管的是**交付前**（這塊東西夠不夠格交出去）。

### 🔴 判準先寫，不得先射箭後畫靶（2026-08-05 使用者定案）

**動工前**就要把判準寫下來：門檻值、量測範圍、什麼叫過。
量完才訂判準，等於拿結果去反推標準——那不是驗證，是自我背書。

- 判準寫在**規則檔或規格書**（本節的四項門檻即為預設判準），不是寫在回報裡。
- 量測**之後**若發現判準或範圍需要改（例如量到才發現範圍抓錯），
  必須三件事一起做：**① 明說是量完才改的 ② 寫明理由與影響 ③ 交使用者裁決**。
  不得無聲調整數字或範圍後宣告通過。
- 測試紅了要先問「是實作錯，還是契約真的變了」。契約真的變才改測試，
  且要在測試裡寫明**為何改**；為了讓它變綠而改斷言＝把守門的拆掉。

⚠ 本節四項門檻**本身**就是 2026-08-05 那次「量完才訂」的產物（lint 0／覆蓋率 90%／
CC ≤ B，正好對上當次結果 0／93%／A–B），複雜度的量測範圍也是量到不過才改成
「只看本次動到的函式」。判準已就此固定下來，日後**只能事前調整**；
下一個模組跑完若覺得門檻不合理，是下下個模組才改，不是當場改。

### 一、功能正確性驗證

**1. 單元測試**——針對最小功能單位（優先抽出純函式：無 I/O、可完全決定性驗證）。
驗四件事：輸入處理、核心邏輯、輸出結果、異常狀態。
⚠ 邏輯藏在有 I/O 的大函式裡就驗不到——**先抽純函式再驗**，這也是 CC 降低的同一件事。

**2. 測試案例設計**——三種技法都要用，缺一就是憑感覺挑案例：

| 技法 | 做法 | 本專案實例（#3） |
|---|---|---|
| 等價類劃分 | 把輸入空間切成行為不同的類別，各取代表值 | joint 佔 total 比例切四類：無／部分／全部／非法(joint>total) |
| 邊界值分析 | 每個類界的 0／1／n−1／n／n+1 逐點踩 | 斜紋在 0／等於段長／超過段長；負值；None 與缺鍵視為同一類 |
| 決策表 | 多條件組合逐格驗證，不只驗 happy path | 註記 8 格（有共同者 × 有受讓人 × 開關）；版本比對 8 格（含值為 None、型別異常） |

⚠ **誠實標示測試性質**：第一次跑就全過的，是**確認性**測試（證明實作已具防禦），
不是除錯性測試——回報時要講清楚，不得讓人以為「抓到並修好了很多問題」。

**3. 回歸測試**——依「回歸只跑範圍」規則跑。
⚠ 契約真的改變時**更新舊測試、不是刪掉**，且每一處都要寫明契約為何改
（本次 7 處：介面參數、青括號、三元組解包）。刪掉等於把守門的東西拆了。

### 二、程式品質與結構驗證

**1. 程式碼審查**（自審＋工具佐證，不能只有自我感覺）：
單一職責／過度耦合／重複程式碼／命名與結構可讀性。
⚠ 審完要**真的改**：本次審出兩處（繪圖與版面計算混在一起、註記兩段重複邏輯），
都實際重構了，不是寫在報告裡就算。

**2. 靜態程式分析**

```bash
uv run --no-project --python 3.12 --with ruff ruff check --output-format json <改動檔> > ruff.json
git diff HEAD~1 -U0 > diff.txt   # 再以行號交集算出「哪些落在本次新增行」
```

🔴 **必須把新問題與既有問題分開**：本專案全庫 ruff 報 742 個問題，
直接看總數什麼都判斷不了。用 **diff 行號交集**界定範圍後才看得出
「本次新增行 5 個 → 修完 0 個」。
⚠ 既有問題中若發現真缺陷（本次找到 4 個：F821 未定義名稱、B023 閉包捕捉迴圈變數等），
**記錄但不順手改**——不在本次授權範圍，改了就是夾帶。

**3. 軟體度量**

```bash
uv run --no-project --python 3.12 --with radon radon cc -s <檔>   # 圈複雜度
uv run --no-project --python 3.12 --with radon radon mi <檔>      # 可維護性指數
```

門檻：**新增／修改的函式 CC 落在 A–B（≤10）**；超過要嘛重構、要嘛在回報中說明理由。
⚠ 重構是為了**真的更好讀**（責任分離、去重），不是為了把數字壓下去——
本次兩處重構同時解掉「重複邏輯」與「兩件事混在一個函式」，複雜度下降是結果不是目的。

**4. 測試完整性**

```bash
uv run --with coverage python -m coverage run --source=<套件> -m pytest <測試> -q -p no:cacheprovider
uv run --with coverage python -m coverage json -o coverage.json
# 再與 diff 行號交集，算「新增行覆蓋率」
```

🔴 **整檔覆蓋率沒有意義**：本專案 `chart_runner.py` 整檔 22%，因為裡面九成是既有程式；
要看的是**本次新增行的覆蓋率**（本次 93%）。
門檻：新增行覆蓋率 **≥90%**；未覆蓋的行要逐行說明為什麼（例如需真實 DB／檔案系統，
且其分支已用等效邏輯測到）。

### 驗收門檻（四項全過才算模組完成）

1. 核心功能測試全過、業務規則符合、異常輸入正確處理、既有功能維持正常
2. **本次新增行**的靜態分析問題 **＝ 0**（既有問題另計、不混報）
3. 新增／修改函式 CC ≤ B（10），超過需說明
4. **新增行**測試覆蓋率 ≥ 90%，未覆蓋處逐行交代

### 工具原則

ruff／radon／coverage 一律用 `uv run --with` **暫時性執行**，
不裝進專案環境、不落系統。⚠ 要常設安裝一律先問使用者（見全域規則）。

（試行條款：先只作本專案層級規則；跑一段時間驗收後再決定是否升格 `D:\力山\.ai-rules\`。）

## Token 節制（本專案試行，自 2026-07-21）

- 小型**已定位**任務以約 3,000 output tokens 為軟目標；migration、跨表、資料安全相關工作以**安全完成優先，不設硬上限**。
- 只削減四樣：全庫掃描、重複讀檔、長 traceback、冗長回報。
- **不得削減**：必要契約測試、資料搬移驗證、downgrade 驗證。
- 同一失敗最多修兩輪，仍失敗即停止並回報阻塞。

（試行條款：先只作本專案層級規則；跑一段時間驗收後再決定是否升格 `D:\力山\.ai-rules\`。）

## Skill 撰寫硬規範（2026-07-21 起，所有 agent 一體適用）

- 本專案所有 skill **最終都會佈署到使用者電腦**，必須讓「零專案背景的 CLI」看了就能執行——這是使用者反覆重申的既有要求，不是建議。
- 每份 skill 固定兩區：**「執行 Runbook」在前**（觸發時機、逐步驟、呼叫的 MCP 工具名、輸入輸出契約、閘門呈現方式；不得出現開發機路徑、直連 DB 指令、migration 編號等開發上下文；依賴工具尚未存在時明確標「待工具：<名>」）；**「開發備註」在後**（設計原因、決策沿革、環境細節）。
- 新增或修改 skill 未照此結構＝驗收不過。Installer 打包＝機械抽取 Runbook 區，不得需要人工改寫。
