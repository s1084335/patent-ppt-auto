# Tasks: 簡報漸進預覽與單頁編輯

分支 `feat/deck-progressive-preview`（自 `feat/sync-report-contracts` 分出）。

⚠ **分支基準的說明**：交付主幹要求「從最新主線建立」，但 `feat/sync-report-contracts`
領先 master 120 個提交且未合併，而本 change 要動的 `index.html`／
`ai_report_deck_runner`／`check_content` 全在輪一大改過。從 master 開等於對著
120 個提交前的程式寫規格。輪一合進 master 後本分支自然乾淨 rebase。

---

## 0. 前置查證（已完成，證據見 design）

- [x] 0.1 量整份重組耗時 → **1.8 秒**（`#426` 素材 18 頁，含 pptx 2.2MB ＋ SVG 18 頁）
- [x] 0.2 確認 `build`／`build_svg` 無逐頁介面 → 整份組版
- [x] 0.3 盤 `check_content` 六道檢查，至少三道本質需全份
      （涵蓋率分母、封面對帳、同頁互斥）
- [x] 0.4 查 SSE 現有形狀 → `LISTEN patent_events` → Queue fanout → `data:` yield
- [x] 0.5 查 ppt-master `svg_editor`（5,234 行、編 SVG 標註屬性）與
      `confirm_ui`（產製**前**的模板選擇）→ 兩者皆不引進，理由見 design §4

## 1. 逐頁事件（TDD）

- [ ] 1.1 Red：`deck_page` 事件 payload 契約——含 `run_id`／`version`／`page`／
      `total`／`asset`；⚠ 反向測試：payload **不得**內嵌圖片位元組
- [ ] 1.2 Red：payload 序列化後 **< 8000 bytes**（`NOTIFY` 上限）
      ⚠ 用最長的實際版本名與路徑測，不用短字串測過關
- [ ] 1.3 Green：runner 在 `build_svg` 後逐頁發事件
- [ ] 1.4 ⚠ 目視迴圈**不逐輪推**、跑完**整批更新**——兩個方向都要測：
      迴圈中不得有事件；迴圈後受影響的頁必須有事件
- [ ] 1.5 ⚠ 失敗保留已推的頁：job 標 failed 後，已發出的事件不得被撤回
      （反向測試：模擬中途失敗，斷言前面的頁仍可取得）

## 2. 單頁編輯後端（TDD）

- [ ] 2.1 Red：只改第 N 頁的 content，重組後**其餘每頁 SVG 逐位元組相同**
      ⚠ 這是 design §1.1 的可驗證推論；不成立代表組版有隱藏跨頁耦合，
      屆時**停止**並回報，不得改斷言遷就
- [ ] 2.2 Green：合併 content patch → 全份重組
- [ ] 2.3 ⚠ 重跑 `check_content` ＋ `audit_deck`（全份），**不**重跑 CLI 目視
      ——兩個方向都測：閘門確實跑了、目視確實沒跑
- [ ] 2.4 ⚠ `make_deck` 的溢出回傳碼必須**呈現**不得吞掉
      （反向測試：造一個會溢出的編輯，斷言使用者拿得到該訊息）
- [ ] 2.5 稽核留痕：改前 hash／改後 hash／頁號／時間／編輯者
      ⚠ 優先沿用既有 manifest；容不下才加 migration，**且要在本檔揭露**

## 3. 前端（現有「匯出報告」頁內）

- [ ] 3.1 deck 紀錄區加逐頁預覽——就地顯示，不新增路由
- [ ] 3.2 逐頁事件到達即更新該頁；顯示 `N/總數`
- [ ] 3.3 該頁就地展開 content 編輯區
- [ ] 3.4 ⚠ 送出後把閘門結果與溢出訊息顯示在該頁旁，不是丟到別處
- [ ] 3.5 前端 inline JS 改動必跑 `test_frontend_js_syntax`（專案硬性規則）

## 4. 驗收

- [ ] 4.1 OpenSpec strict validation
      ⚠ **可能阻塞**：`openspec` CLI 目前不在 PATH（2026-08-19 實查）
- [ ] 4.2 三層範圍回歸：直接測試 ＋ 消費它們的整合測試 ＋ 契約測試
      （`test_frontend_js_syntax`、SSE 契約、deck runner）
- [ ] 4.3 🔴 實機端到端：真跑一次 deck，**逐頁事件在產製中真的一頁一頁出現**
      ⚠ 不是「測試綠」——要真的看到畫面逐頁長出來
- [ ] 4.4 🔴 實機單頁編輯：改一個字 → 重組 → **確認其餘頁未變**、閘門有跑、
      目視沒跑、耗時符合 1.8 秒量級
- [ ] 4.5 揭露未覆蓋範圍（見下）
- [ ] 4.6 使用者接受後 archive；同步 main specs

## 5. 已知未涵蓋（明文揭露，不藏在綠燈裡）

| 未涵蓋 | 為什麼 | 何時要處理 |
|---|---|---|
| **並行編輯** | 目前單人使用情境，預設「後寫入者覆蓋」 | 一旦多人同時用，此假設不成立就要重新設計 |
| **編輯歷史回溯** | 只記最近一次改前 hash，不做版本樹 | 需要「回到三次修改前」時 |
| **AI 套用標註** | ppt-master 的標註模型值得借鑑，但那是另一條線 | 想讓 AI 依人的標註自動改時 |
| **窄轉換器接上** | 獨立決定，見 `decisions.md` 2026-08-20 更正段 | ⚠ 若接上，重組的成本結構會變（不走 python-pptx），但本設計不阻礙 |

## 6. 相依與時序

⚠ 本 change **不阻塞**輪一，但實機驗收（4.3／4.4）**依賴輪一 §8.4**
——要先有一次成功產出的 deck 才有東西可預覽與編輯。

建議順序：輪一 §8.4 過 → 本 change 1.x／2.x 實作 → 3.x 前端 → 4.x 驗收。
