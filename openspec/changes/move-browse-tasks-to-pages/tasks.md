# Tasks: 瀏覽區作業頁面化

⚠ 本 change 是**純搬遷、零新功能**。任何行為差異都是缺陷，不是「順便改進」。
⚠ 兩塊各自獨立切片——一次搬一塊，壞了立刻知道是哪一塊。

## 0. 基線

- [ ] 0.1 測試基線：`pytest tests -q` 的 passed／failed 數與既有紅清單
- [ ] 0.2 **搬遷前的行為快照**（判準 1 的對照組）：
      - 公司名治理：建組、AI 建議、待審覆核、未歸戶名稱各走一次，記錄輸出
      - TW 狀態：載入待登錄清單、成功登錄一筆、非法值被拒、重複提交衝突
      ⚠ 沒有這份快照就沒有「等價」可言，只剩「看起來一樣」
- [ ] 0.3 記錄兩個待辦數的當前值與其來源端點

## 切片 A：入口卡片共用元件

- [ ] A.1 Red：入口顯示待辦數且數字與後端端點一致（WSP-014）
- [ ] A.2 Red：前端不自行計算待辦數（斷言計數來源）
- [ ] A.3 Red：待辦為 0 時入口仍顯示且可進入（WSP-014）
- [ ] A.4 Green：抽出入口卡片元件
- [ ] A.5 Refactor：輪一建立的初階篩選入口改用同一份元件
      ⚠ 三個實例才抽象——此時形狀才看得出來（見 design）
- [ ] A.6 驗收：三個入口外觀與行為一致

## 切片 B：專利權人代碼與中文名搬頁

- [ ] B.1 Red：新頁可自瀏覽入口進入、且不出現在左導覽（WSP-014）
- [ ] B.2 Green：`renderMain()` 加分派；新頁進入點呼叫**既有那 65 支函式**
      ⚠ **只改掛載點，不動函式本體**
- [ ] B.3 Green：瀏覽首頁移除該區塊，改為帶待辦數（未歸戶筆數）的入口
- [ ] B.4 ⚠ 若發現非改函式本體不可才搬得動——**停下重新評估耦合**，不硬拆，
      並在本檔記下原因與日期
- [ ] B.5 驗收（對照 0.2 快照）：建組、AI 建議、待審覆核、未歸戶名稱**逐項比對**
      搬前搬後輸出
- [ ] B.6 回歸：`company-governance` 相關測試全綠（該 spec 21 條需求無一綁定介面位置，
      故不應有行為變化）

## 切片 C：TW 專利狀態管理搬頁

- [ ] C.1 Red：新頁可自入口進入、不在左導覽；入口顯示待登錄件數（WSP-012、WSP-014）
- [ ] C.2 Green：`renderMain()` 加分派；新頁沿用既有清單與登錄邏輯
- [ ] C.3 Green：瀏覽首頁移除該收合區塊，改為入口
- [ ] C.4 驗收（對照 0.2 快照）：`WSP-012` 其餘五個 scenario **全部重驗**
      ——只列待登錄、九項值域、成功登錄、非法／非 TW 被拒、重複提交衝突、儲存失敗保留列
- [ ] C.5 ⚠ 特別驗「儲存失敗保留該列與使用者選擇」：搬頁最容易弄丟的就是這種
      錯誤路徑的狀態保留

## 切片 D：瀏覽首頁收斂

- [ ] D.1 Red：文獻備註仍在瀏覽首頁（判準 6）
- [ ] D.2 Green：首頁收斂為 搜尋 ＋ 三個入口 ＋ 文獻備註 ＋ 專利表
- [ ] D.3 驗收：進頁面第一眼看得到專利表（本 change 的初衷）

## 1. 範圍回歸

- [ ] 1.1 直接測試：兩個新頁與入口的斷言
- [ ] 1.2 整合測試：公司治理、TW 狀態的既有測試
- [ ] 1.3 契約測試：`test_api_frontend.py`、`test_frontend_js_syntax.py`
- [ ] 1.4 ⚠ **既有斷言只能改不能放寬**。因掛載點變更而需改的斷言，逐處註明理由；
      任何「先註解掉再說」一律不接受
- [ ] 1.5 完整套件，與 0.1 基線逐項比對

## 2. 交付前驗收

- [ ] 2.1 逐項對照 proposal 的 Acceptance Gate 8 條
- [ ] 2.2 ⚠ 判準 1（搬遷前後功能等價）以 0.2 的快照**逐項比對**，不接受抽樣
- [ ] 2.3 OpenSpec strict 通過
- [ ] 2.4 未執行與不適用的項目分開揭露

## 3. 收尾

- [ ] 3.1 回寫本 tasks：實測與規劃不符處註明原因與日期
- [ ] 3.2 更新 `.agents/context/` 相關唯一來源
- [ ] 3.3 使用者明確接受後才 archive
