# Tasks: sync-report-contracts-and-palette

⚠ 閘門進場前先過 design §1 三問（不看語意能否判定／恆等式 vs 代理指標／
偏差是多出來還是缺席）。本 change 有三個閘門，§1 已逐一過過。

⚠ 全批採 TDD：先寫會紅的測試，再最小實作。閘門類任務**必須做反向驗證**
（把正確的東西改壞，閘門要紅）——空閘門比沒閘門更糟。

⚠ 本 change 未經使用者確認前不得 apply。

## 🔴 執行分段（2026-08-18 使用者裁決：分 4 段，逐段回報）

切點是**依賴**與**能不能獨立驗收**，不是平均分。

| 段 | 節 | 項數 | 段末可驗的實物 |
|---|---|---|---|
| **1. 母體** | §1 ＋ §2 ＋ §7e | 14 | 兩個 workspace 數字**不相等且各自正確**（滑雪機 55/48、割草機 226/163）；拿掉 `patent_ids` 閘門要紅 |
| **2. 版型庫** | §7a → §7b → §7c → §7d | 15 | 三處同步閘門綠；`conclusions` **真的產得出那一頁**；`roadmap` 不再出現 |
| **3. 收斂** | §6 ＋ §4 | 18 | 裸 hex 為 0；「造形」語意字串逐處人工過目未被改動 |
| **3.5 結論解鎖** | §9 | 待估 | 建議數量由資料決定；每項行動可反向追溯到引擎資料 |
| **4. 契約與驗收** | §3 ＋ §5 ＋ §8 | 16 | `report_key` 對帳閘門綠；兩份報表全頁目視 |

⚠ §9 於 2026-08-19 由使用者提出並排在第 3 段之後、第 4 段之前——它會改版面與閘門，
而 §5 文件同步必須看得到最終版面，放在 §5 之前才不用同步兩次。
⚠ 第 3 段項數由 10 修正為 18：§6 實查後把「一句話的任務」展開成可執行步驟
（6.2a–6.2f、6.3a、6.3b、6.7）。這是展開不是範圍蔓延，但段的大小確實變了。

### 為什麼這樣切（不要自行調換）

- ⚠ **§1 必須最先**：§7e 的法律狀態分解是**新的彙總**。閘門立起來之前做它，
  等於當場再種一個同型錯誤（該錯已出現三次）。§2 封面數字同理。
- ⚠ **§5 文件契約必須最後**（不直覺）：第 2 段會改 `narrative.md`、`SKILL.md`、
  `content-template.json`，第 3 段會改用詞。文件同步排前面就要**同步兩次**，
  第二次一定有人漏。
- ⚠ **§4 用詞單獨放第 3 段**：全批**唯一不可逆**的一節（「外觀」有兩種語意，
  誤改文獻備註救不回來）。不能趕，也不該跟別的事混在同一段的注意力裡。
- **第 2 段內部有順序**：7a 的反向驗證會當場抓出還有哪些版型漏登記；
  7c 要在 7d 前（7d 的結論頁要用通用表格版型）。

### 每段結束要交的東西

改了哪些檔／跑了哪些測試／**反向驗證結果**（把對的改壞，閘門有沒有紅）／
這段沒驗到什麼／下一段開工前需不需要使用者裁決。

⚠ §6 的色票數字（28→49 種）是觀測值不是現況，第 3 段開工時**重新實查**。

**2026-08-19 使用者再確認**：§4 用詞**照原規劃留在第 3 段**，與 §6 色票同段完成，
不抽出、不延後。⚠ 同段混了一個可逆（色票）與一個不可逆（用詞）的改動，所以
第 3 段的**兩件事要分開 commit**——用詞那批單獨一個 commit，出事才回退得乾淨。

---

## 1. 母體閘門（最優先——同型錯誤已出現三次）

- [x] 1.1 掃出所有繞過 `run_report` 自行 `cur.execute` 的彙總，列成嫌疑清單
      （`scripts/audit_population_scope.py`，唯讀）

      **結果與原假設不同，記在這裡供後續不要重查：**

      第一類（繞過 `run_report` 自行查）：14 個函式，10 個未見母體條件。
      逐個人工判定後**只有 1 個是真 bug**：

      | 函式 | 判定 |
      |---|---|
      | `chart_runner.fetch_patent_kind_summary` | 🔴 真 bug（見 1.4） |
      | `mcp_server.get_data_status` | 全庫用途**正確**，但見 1.7 |
      | `list_zh_name_drafts`／`count_company_normalization_queue`／`list_company_groups`／`list_confirmed_group_candidates` | 公司治理跨 workspace，**本來就該全庫** |
      | `refresh_patent_search_terms`／`refresh_report_patent_base` | derived 重建，全庫 |
      | `api/jobs.ready` | 健康檢查，全庫 |
      | `workflow_outputs_repository._append` | 誤報（不是報表彙總） |

      ⚠ **掃描器有盲點**：它只抓「繞過 `run_report`」的，抓不到「走 `run_report`
      但定義 `supports_patent_ids=False`」那一類——第 2 例（受理局家族註記）就是
      那類。第二類要另外掃 `report_definitions.py`。

      第二類（`supports_patent_ids=False`）：4 個。

      | 報表 | 判定 |
      |---|---|
      | `family_country_layout` | 🔴 真 bug（見 1.5） |
      | `applicant_strength_profile`／`cluster_topic_table`／`opportunity_quadrant` | ✅ **排除嫌疑**——`report_type="cluster"`，範圍由 `workspace_id` 經 `load_cluster_workspace_data` 給，不是由 `patent_ids`。實測滑雪機 ws=3：成員 55 → 指派專利 **44**（44 ≤ 55，沒有洩到全庫） |

      ⚠ 那個 **44** 是「11 件外觀設計被靜默排除」（分不了群），屬 **deepen §3**
      而非本節——同樣是母體不對，但根因相反：§1 是**洩到全庫**，deepen §3 是
      **靜默縮小**。不要在本節「修」它。

- [x] 1.7 `get_data_status` 的全庫數字要**標明是全庫**
      ⚠ 它不是 bug，但是現成的誤讀陷阱：CLI 讀到 `patents: 281` 可能當成本報告母體，
      那正是封面 281 的形態。純加標籤，不改數字。
- [x] 1.2 閘門：`backend/app/db/population_scope.py`（掃描邏輯唯一定義處，
      測試與 script 共用一份）＋`tests/test_population_scope_gate.py`
- [x] 1.3 白名單：模組層 `POPULATION_SCOPE_EXEMPT` 宣告＋理由必填（空字串不算）；
      測試另存 `REVIEWED_EXEMPTIONS`，新增豁免要同時改兩處，讓它不可能悄悄長大
      ⚠ 三問：Q1 過、Q2 **不過**（塞進豁免表就通關＝代理指標）、Q3 過（diff 看得見）。
      保證「每個全庫彙總都被登記過」，**不保證理由是對的**
- [x] 1.4 `fetch_patent_kind_summary` 接 `patent_ids`
      （原況全庫 281 件／設計 21，滑雪機實際 55／11——2.5 已實測確認）
      ⚠ 參數做成 **keyword-only 且無預設**：忘記傳是 TypeError 當場炸，
      不是靜默退回全庫（那正是本 bug 的形狀）
- [x] 1.5 修受理局頁家族註記
      🔴 **2026-08-18 實測推翻原判定，原文保留在此以免下次重犯：**
      ~~不得用 `family_country_layout`（現況全庫 187，滑雪機實際 48）~~

      **實際不是母體問題。** 母體本來就有接——`ChartContext.report()` 一律傳
      `patent_ids`，而 `supports_patent_ids=False` 的語意不是「忽略母體」，
      是「**家族層報表：母體由 `build_family_scope_clause` 翻譯成家族集合**」
      （`report_engine.py` 353–360）。
      ⚠ `report_family_country` **沒有 `patent_id` 欄**，把該旗標改成 True 會產生
      `patent_id = ANY(...)` 直接讓 SQL 壞掉。已加測試守住不要被改。

      **真正的缺陷是加總錯誤**：報表依國家 group by，每列是「該國有幾個家族」，
      相加等於同一家族跨幾國算幾次。

      | workspace | 各國相加（註記原本顯示） | DISTINCT 家族 | 國家數 |
      |---|---|---|---|
      | 滑雪機 | **46** | **40** | 4 |
      | 割草機 | 159 | 144 | 2 |

      ⚠ 那個 **46** 已經以「存活 46」的形式傳進 deepen 的文件——錯數字會擴散。

      修法：註記改講自己算得準的東西（佈局點數＋涵蓋國數），並明說跨國會重複計入；
      家族總數的權威口徑交給封面（§2）。**187 那個數字應是 08-17 引擎母體修好之前
      量的，已過時。**

- [x] 1.5b 家族口徑三個數字**已由 §2.2 收斂**
      滑雪機：`report_family_country` DISTINCT = **40**；各國相加 = **46**
      （deepen 文件記載的「存活 46」就是它）；`report_patent_base` 的
      `COUNT(DISTINCT 家族ID)` 於母體 = **48**。
      → **封面採 48**（§2.2）；46 已在 1.5 修掉來源，並換掉 `narrative.md` 的錯範例；
      40 是 `report_family_country` 自己的語意（有保護國家列的家族），不上封面。
- [x] 1.6 反向驗證：**5 則變異全數轉紅**——拿掉 WHERE／加回預設值／呼叫端傳 None／
      傳空清單／卸錯豁免；還原後全綠
      ⚠ 過程中抓到**我自己測試的假性通過**：原本只斷言 `patent_ids=` 出現，
      改成 `patent_ids=None` 照樣綠。已改為指名 `ctx.patent_ids`

## 2. 封面數字由引擎供給

- [x] 2.1 `report_data.cover_stats`（件／族／受理局／三分法）；`patent_ids` 必填無預設
- [x] 2.2 家族數口徑＝`FAMILY_ID_EXPRESSION` 於母體（沿用唯一定義處，不另寫）。滑雪機 48
      ⚠ 缺同族 ID 時各自算一族，不得併成一族「未知」
- [x] 2.3 三分法委派 `fetch_patent_kind_summary`；測試斷言封面不得出現 `document_kind`
- [x] 2.4 範本 `stats_note` 移除「存活家族」；第四格改專利類型；`narrative.md` 的「存活家族 46」錯範例一併換掉
- [x] 2.5 兩個 workspace 各驗一次（實測 9/9 全中）：滑雪機 55／48／4／17·27·11；
      割草機 226／163／2／151·65·10 —— **兩者不得相等**

## 3. 家族數落點收斂

- [x] 3.1 Red：`annual_trend`／`publication_trend` 的顯示欄不含 `family_count`
- [x] 3.2 登記隱藏欄；⚠ `chart_rows` 必須保留資料（CLI 取證要用）
- [x] 3.3 確認 KP 表與 KP 象限泡泡不受影響（per-applicant 維度，不動）

## 4. 用詞統一為「設計」

⚠ **分類後才改，不得全域取代**（design §5）：「外觀」有兩種語意，
專利類型改名、產品造形保留。誤改會破壞文獻備註語意且不可逆。

### 🔴 4.0 分類實查結果（2026-08-19，取代上述風險假設）

逐處掃完 **112 處**（已排除 `openspec/archive/`）：

| 語意 | 處數 |
|---|---|
| 專利**類型** | **112** |
| 產品**造形** | **0** |

⚠ **源碼裡沒有造形語意**。最接近的 `narrative.md`「外觀可能補足產品形態保護」
仍是在講外觀設計這個**類型**保護產品形態，主詞是類型。

真正的風險**不在源碼在執行期資料**——文獻備註與摘要原文（來自專利公開文字）
可能含「外觀」指造形。本節只改標籤與常數、**不碰資料**，該風險不成立。

**改動規則收斂成一句：只保留「外觀設計」這個四字法律名詞（CN 正式用語），
其餘一律改。** 殘留 6 處逐處確認全是法律名詞。

⚠ 第一版只換 4.2 列舉的 3 個字串，造成**同一句新舊混用**
（「設計保護策略：申請人 × 技術／外觀／技術+設計」）——混用比不改更糟；
而且漏了 4.2 明確點名的欄名對照與一個輸出字串（`"外觀先行 N 年"`）。

⚠ 使用者 2026-08-19 確認：「外觀就是用專利種類啊，就算改設計邏輯還是一樣」
——純用詞，邏輯不動。

### 4.5 跨語言重複（實作時發現，已處理）

- [x] 4.5.1 `index.html` 自己也寫了一次 `['design_protection_detail', '外觀保護策略']`
      ——只改後端畫面會繼續顯示舊名且**不會有任何東西報錯**。與色票那次
      （前端 CSS 變數）同型，處理方式一致：兩邊都改 ＋ 一致性測試

- [x] 4.1 逐處分類 46（後端）＋60（測試）＋7（prompt）＋5（deck）處，
      標記「類型」或「造形」
- [x] 4.2 只改「類型」那組：`DESIGN_STRATEGY_AXIS`、欄名對照、
      `strategy_type` 值（`只走外觀`→`只走設計`、`技術+外觀`→`技術+設計`）、
      報表 `label_zh`（外觀保護策略→設計保護策略）
- [x] 4.3 ⚠ OpenSpec `archive/` 底下**不改**（歷史紀錄）
- [x] 4.4 一致性測試：三分法標籤與報表用詞一致；
      「造形」語意的字串未被改動（逐處人工過目，不只跑測試）

## 5. 文件契約同步

- [x] 5.1 在 design §2 的權責邊界表基礎上，逐檔標出「這段屬於誰的職責」
- [x] 5.2 Red：`report_key` 集合對帳閘門
      —— 文件提到但定義裡沒有＝紅；定義裡有但文件沒提＝黃（列出不擋）
- [x] 5.3 反向驗證：文件塞一個假 report_key 要紅
- [x] 5.8 `test_topic_table_single_render` 兩條改成**驗行為**（2026-08-19 使用者裁決）

      ⚠ **更正 08-19 稍早的錯誤歸屬**：我當時說「anchor 在 chart_runner 全歷史都
      不存在（`git log -S` 查無）」——**那是錯的**。字串一直在（L5208
      `"title": "分群分析"`）。真正的失效原因是 2026-08-12 有人在
      `ctx.sections.append({` 與 `"title":` 之間插了一行**註解**，而 regex 寫的是
      `\(\{\s*"title"`，`\s*` 只容得下空白。`git log -S` 查無是我在 PowerShell
      裡的引號被吃掉，不是歷史真的沒有。

      ⚠ 這是「註解破壞斷言」第 6 次，方向是新的：前五次是註解裡的字**餵出假通過**，
      這次是註解**擋掉真比對**造成假失敗。根因同一個——拿原始碼文字當契約，
      而註解是原始碼文字的一部分。

      ⚠ 失效症狀是「一直紅」不是「突然紅」，於是被當成既有債忽略了好幾週；
      更糟的是 `test_variant_has_no_chart_file` 卡在 `assertIsNotNone`，
      它底下兩條真正的斷言**從來沒被執行過**。

      已改為實跑 `_build_cluster_analytics_section` 再驗 section 的 rows 與
      variant。反向驗證：拿掉 rows／把 variant 指向 SVG 各自轉紅（2/2）。
- [x] 5.4 同步 `prompts/report-narrative-flow.md`（family_country_layout、
      年度矩陣、country_distribution 狀態堆疊、topic_timeline 段落）
- [x] 5.5 同步 `prompts/content_standard.md`（技術主題×2）
- [x] 5.6 同步 `skills/html-report-to-deck/SKILL.md`＋`references/narrative.md`
- [x] 5.7 同步 `add-deck-delivery-line/design.md` 的頁面盤點
      （現存有效×2、主題演進×3、年度矩陣×2、更多、技術主題）

## 6. 色票唯一定義處

### 🔴 6.0 實查結果（2026-08-19，唯讀，取代先前所有觀察值）

腳本 `scripts/audit_palette.py`（見 6.7）。⚠ 先前的 **28 種**（deepen 5b.1 原記）
與 **49 種**（08-18 觀察）都不是現況。

| 量測 | 值 |
|---|---|
| chart 側不同顏色 | **48 種**（出現 82 次：具名常數 33／散落 49） |
| chart 側**完全沒有具名常數**的顏色 | **24 種**（一半） |
| deck 側不同顏色 | **14 種**（具名 12／散落 12） |
| 兩側都出現＝同一份知識兩個落點 | **4 種**（`#00094A`／`#9CA3AF`／`#E5E7EB`／`#FFFFFF`） |

**⚠ 49 處散落不是同一種東西，分屬三個媒介**——這決定 6.3 的唯一定義處要不要分層：

| 媒介 | 處數 | 說明 |
|---|---|---|
| SVG 圖元（函式內區域 dict／字面） | 30 | `_TIER_COLORS`、`qcolors` 等函式內字典，與 `STATUS_COLORS` 同性質卻沒上升成模組常數 |
| HTML 報表 CSS 變數（`:root{--paper…}`） | 15 | 內嵌 f-string 模板，**已經有自己一套命名**（`--paper`／`--ink`／`--brand`），註解還寫著對應舊名（`--text`／`--border`／`--accent`）＝第三套命名 |
| HTML 表格內嵌樣式（source field 檢視頁） | 4 | 除錯用頁面 |

### 🔴 6.0a 實查新發現：顏色被當成語意選擇器（規格沒記，比色票分岔嚴重）

`skills/html-report-to-deck/scripts/rebuild_chip_chart.py` L57／L67：

```python
d["note"]   = next((t for a, _, t in head    if "#9CA3AF" in a), "")
d["footer"] = next((t for a, _, t in reversed(tail) if "#9CA3AF" in a), "")
```

它靠 **`#9CA3AF` 這個色值**辨認「哪一段文字是註記」。chart 側只要換掉那個灰，
這裡不會報錯，只會**靜默回空字串**——註記與頁尾直接消失。
⚠ 這比「兩套深藍」危險：深藍分岔只是看起來不一致，這個是功能會壞而且無聲。

`rebuild_chip_chart.py` 在 deck 側（`skills/`）卻產 SVG、用 chart 側的色，
12 處全部散落、0 個具名——它是兩側耦合最深的一個檔。

### 🔴 6.0b 「不得新增第 29 種」這條判準已經失效

deepen 5b.4 寫「不得新增第 29 種」，但實查是 48 種——在 `COLOR_TRANSFERRED`
（`#7C3AED`，chart_runner L371）之前就已經多出約 20 種。單獨點名它是任意的。

⚠ **數量鎖本來就守不住**（v5／v7／v9 同型）：它沒說「該從哪裡取色」，只說
「不准超過 N」，於是每個人都覺得自己那一個是必要的例外。
判準應改為**結構性**的 6.4：「散落裸 hex 為 0」——那是恆等式（做對＝唯一途徑），
不是代理指標。6.6 因此改成「登記 `COLOR_TRANSFERRED` 進色票表並補語意用途」，
不是撤回它。

- [x] 6.1 盤點：chart 側 hex 與 deck 側 `RGBColor` **跨語法**比對（結果見 6.0）
### 🔴 6.2 兩套深藍：**都留，但不得同頁**（2026-08-19 使用者裁決）

⚠ 先更正規格：deepen 5b.2 寫「肉眼近乎一樣但值不同」——**實測 ΔE2000 = 10.53**，
遠過可辨閾值。差別不在深淺而在彩度（`#00094A` 近純藍、`#0B2545` 偏灰板岩藍）。
兩者在四種實際底色上對比度 11.99–18.45，都遠超 WCAG 4.5，**無障礙不是判準**。

實查落點：`#00094A`＝`chart_runner.COLOR_TEXT` ＋ `rebuild_chip_chart` 5 處散落；
`#0B2545`＝`deck_layout.TEXT`（L44）。現況**每一張圖表頁都同框**：頁標題由
PowerPoint 畫（`#0B2545`），正下方嵌的圖自己的標題是 `#00094A`
（`chart_runner.py:629` `data-role="chart-title"`）。

**分界從「哪個模組」改成「哪個媒介」**：

| 媒介 | 主文字色 | 涵蓋 |
|---|---|---|
| HTML 報表 | `#00094A` | 報表頁面文字 ＋ 報表內嵌的圖 |
| PPTX 簡報 | `#0B2545` | 投影片文字 ＋ **進 deck 的那份圖** |

同一份 SVG 進 deck 時整批換色，於是任一頁面上只會出現一種深藍。
⚠ 這不是「兩個模組各用各的」——那正是現在的狀態，而且它同頁。

- [x] 6.2a 對照表**由來源端產生**：`chart_runner` 把「報表色 → deck 色」寫進
      `report_data.palette`；deck 側只讀不定義。
      ⚠ 跨部署單元不能 import（`skills/` 可攜），走「一方產生、一方消費」；
      比照今晚 `cover_stats` 的 intake 模式，不新增第三份定義
- [x] 6.2b 換色步驟掛在既有 SVG 轉換鏈（`apply_chart_marks` 之後、
      `fit_render_charts` 之前）。⚠ **必須在 6.3a 之後**——`rebuild_chip_chart`
      拿 `#9CA3AF` 當語意選擇器，先換色會讓它靜默回空字串
- [x] 6.2c 閘門驗**產物**不驗原始碼：進 deck 的 SVG 與產出的 `.pptx` XML 裡，
      對照表左欄（報表色）出現次數必須為 0；HTML 報表產物反之。
      ⚠ 這是恆等式——要滿足它**唯一**途徑就是真的換了色。斷言「原始碼有呼叫
      換色函式」是代理指標（本專案已踩過：函式在、字串在、資料到不了照樣綠）
- [x] 6.2d **缺席要現形**：閘門同時列出「兩張色票都沒有」的色，不是靜默放行。
      ⚠ 只擋已知的左欄＝新冒出來的第三種藍不會被發現（缺席型偏差）
- [x] 6.2e ❌ **不設**「每頁只能有一種藍」的目視規則，也不設色票數量上限。
      前者無法機械判定、只能靠人看，而且可以用「把圖拿掉」滿足（v5／v7／v9
      形式鎖同型）；後者已於 6.0b 證明守不住

#### 6.2c 這道閘門自己先過三問（design §1.2）

| 問 | 判定 |
|---|---|
| ① 不看語意能否判定？ | **過**。數對照表左欄在產物裡的出現次數，純字串比對 |
| ② 滿足它的**唯一**途徑是否＝把事情做對？ | **不過（有洞，見下）** |
| ③ 偏差是多出來的（可見）還是缺席的（兜不住）？ | 6.2c 只擋已知左欄＝缺席型，故必須配 6.2d 把未知色列出來 |

⚠ **第二問的洞**：「deck 產物不得出現報表色」也可以用**把那張圖整個拿掉**來滿足
——那正是 v5／v7／v9 三次形式鎖的死法（為過鎖而刪內容，而且刪掉的不會留痕跡）。

- [x] 6.2f 補上恆等式的另一半：換色閘門必須**與既有的圖數／頁數檢查同時綠**，
      並斷言換色前後**圖檔數量與每張圖的文字節點數不變**。
      ⚠ 這樣「拿掉圖」與「拿掉文字」都會當場紅，滿足閘門的唯一途徑才真的
      只剩「把色換對」。沒有這一條，6.2c 就是一個可以用刪除滿足的鎖
- [x] 6.3 定唯一定義處（比照字型定在 `chart_sizing`）
      ⚠ 依 6.0 的三媒介，唯一定義處要能同時服務 hex 字串（SVG／CSS）與
      `RGBColor`（PPTX）。⚠ 跨部署單元不能 import（`skills/` 是可攜的），
      `rebuild_chip_chart` 那條走「一方產生、一方消費」，不是 import
      ⚠ 6.2 定案後，定義處要裝的是**兩張色票＋一份對照表**，不是一張色票。
      色票條目必須帶「媒介」欄（`report`／`deck`／`both`）——4 種兩側共用的色
      （`#FFFFFF`／`#9CA3AF`／`#E5E7EB`／`#00094A`）中，前三個是真的共用、
      第四個是待換掉的，兩者不分開就會把 bug 當成設計
- [x] 6.3b 命名要說得出媒介：`COLOR_TEXT`／`TEXT` 兩個名字都看不出差別，
      是今天會同頁的原因之一。改成能自我說明的（例如「圖內文字」與「頁面文字」）。
      ⚠ 這條**不是**改名潔癖：`chart_runner` 已有先例——`TEXT_ON_LIGHT` 的註解
      特地寫「那是頁面文字，這是畫在圖元上的文字，底色來源不同」，
      表示這個區分早就存在，只是沒有反映在名字上，於是每次都要靠讀註解
- [x] 6.3a 🔴 先解 6.0a 的**顏色當語意選擇器**（`rebuild_chip_chart` L57／L67）
      ——它會讓 6.2 的收斂變成靜默破壞。⚠ **排在 6.2 之前做**

      已完成（2026-08-19）：角色標記 `ROLE_CHART_NOTE`／`ROLE_CHART_FOOTER`
      定在 `chart_sizing`（與字型同一個唯一定義處），產生端 `chart_runner` 打、
      消費端 `rebuild_chip_chart` 讀，**輸出端也打**（重排後的圖不能失去語意標記）。
      ⚠ 順帶更正 `parse` 的 docstring：它原本寫「不比對任何特定字串」，
      而實作正在比對 `#9CA3AF`——宣稱的護欄與實作不符。

      關鍵測試是 `test_parse_survives_recolor`：**把 §6.2 的換色做進測試裡**。
      只斷言「有 data-role」不夠，那只是換一種字串比對；要證明的是
      「顏色變了、解析仍然對」。反向驗證 5 則全轉紅（拿掉 note 標記／拿掉
      footer 標記／改回用顏色選／輸出端不打標記／角色名打重複）。

      ⚠ **不驗完整往返**（`parse(build(...))`）：`build` 吐的象限方塊與 `parse`
      期望的格式本來就不同，這支腳本設計是對引擎原圖跑一次、就地覆寫，
      從不支援讀自己的輸出。要求它往返是測過頭，會逼出沒人需要的相容層。
- [x] 6.4 硬判準：`chart_runner` 散落裸 hex 為 0（模組層具名常數除外）
      基線 **49 處**（SVG 30／HTML CSS 15／表格樣式 4）。
      ⚠ 掃描器必須用 `ast` 取常數行範圍——正則＋括號計數會被內嵌 CSS 的
      `{{ }}` 帶歪（實測誤判成 68、61，真值 49）
- [x] 6.5 軟揭露：色票表列出每個色的語意用途，新增時必須填
      ⚠ 24 種色目前**完全沒有具名常數**，補表時等於要為它們命名並寫用途；
      這是本節工作量的主體，不是 6.2
- [x] 6.6 登記 `COLOR_TRANSFERRED` 進色票表並補語意用途
      （原文「補回…違反 5b.4 的新增」；5b.4 的數量鎖已失效，理由見 6.0b）
- [x] 6.7 實查腳本落地成 `scripts/audit_palette.py`（唯讀），讓 6.4 的閘門與
      人工盤點讀同一份掃描邏輯，不各寫一份

### 6.5b ✅ 色彩庫是**給 CLI 挑的**（2026-08-19 使用者提醒，已完成）

> 「色彩庫和版型庫本來就是要給 CLI 去選擇的，這個要記得。」

⚠ 前兩批我把色票當**引擎內部常數**在收（`chart_sizing.PALETTE`／`SCALES`），
那部分是對的，但**漏了 CLI 面向的那一半**。兩層要分清楚：

| 層 | 誰用 | 落點 |
|---|---|---|
| 引擎內部色／色階 | `chart_runner` 畫圖 | `chart_sizing.PALETTE`／`SCALES` |
| **CLI 可挑的色** | CLI 寫進 `content.json` | `deck_layout.COLOR_LIBRARY` |

- [x] 6.5b-1 `COLOR_LIBRARY` 每項帶「什麼情況用」；`PALETTE` 由它導出不另寫一份
- [x] 6.5b-2 `check_content` 擋庫外色（原本完全不驗，CLI 寫錯會拖到
      `make_deck` 才 KeyError，錯誤訊息與內容無關、修稿輪也修不掉）
- [x] 6.5b-3 SKILL.md 表格與唯一定義處同步（比照版型庫的三處同步閘門）
- [x] 6.5b-4 清掉 SKILL.md 殘留的 `roadmap`（§7d 第 2 段只清了範本沒清 SKILL；
      CLI 同時讀兩份，等於教它寫一個畫不出來的區塊）＋頁數公式 ＋3 → ＋2

### 6.8 同頁互斥閘門（§6.2 的兩個紅還沒有機械保證）

深藍靠**媒介**分離（`recolor_for_deck` 換色，結構上不可能同頁）；
但 `#C62828`／`#DC2626` **都在報表側**，分不了媒介，只能驗**頁面組成**。
`NOT_SAME_PAGE` 目前只是宣告，還沒有東西在檢查它。

- [x] 6.8.1 閘門：逐頁掃該頁用到的圖，`NOT_SAME_PAGE` 任一對不得同時出現
- [x] 6.8.2 ⚠ 判準是**渲染後的頁**不是圖檔本身——雙圖頁會把兩張圖放同一頁，
      各自合規但合起來違規
- [x] 6.8.3 Red：造一頁同時含兩個紅 → 要紅
- [x] 6.8.4 ⚠ 違規時**不得自動換色**：兩個紅分屬不同色階（生命週期 vs 量級），
      自動換會把語意改掉。閘門只報告，由人決定拆頁或改圖

## 9. 結論數量解鎖與證據鏈（2026-08-19 使用者需求）

> 「結論或研發方向不一定只能提四項，需要的是**完整由資料驅動**所得出的結論或建議。」

### 9.0 實查：「四項」寫死在四層，而且其中一層在說謊

| 層 | 內容 |
|---|---|
| `check_content.py:428` | `if len(recommendations) not in (3, 4, 5)` — 硬鎖 3–5 |
| `deck_layout.slide_rec` | 硬兩欄，且 `need_total = 2 * gh + 0.18` **寫死 2 排** |
| `SKILL.md:156` | 「建議卡用 4 張（2×2）…3 或 5 張的排法**未經回歸驗證**」 |
| `narrative.md:87` | 範例標題「**四類**研發方向」 |
| `content-template.json` | A／B／C／D 四張 |

🔴 **既有 bug**：閘門說允許 5 張，但版面高度只算 2 排。5 張要 3 排，裕度檢查
少算一整排 → 5 張會溢出而閘門不擋。「允許 3–5」是假的，實際只有 4 張安全。

⚠ 數量鎖與 v5／v7／v9 形式鎖、§6.0b「不得新增第 29 種色」同族——已證明守不住。

### 9.1 四則使用者裁決（2026-08-19）

1. **rec 退場，全歸 conclusions**（同一問題不答兩次）
2. **裝不下時自動分頁**（字級鎖死不得縮字；分頁是唯一不損失內容的解法）
3. **完整性判準＝證據鏈可反向追溯**：`分析結果 → 證據 → 結論 → 專利行動`
   四段每段都接得上。⚠ 使用者明確**不要**「至少 N 項」這種數量下限。
4. 排在第 3 段之後、第 4 段之前

### 9.1b 🔴 行動空間完整掃描（2026-08-19 使用者補充設計，取代先前的「母體＝主題」解讀）

使用者原文的流程：

```text
資料分析 → 掃描所有可成立的行動方向 → 每個方向判斷「成立／不成立／證據不足」
        → 只輸出成立的專利行動
```

範例（候選池 6 項）：

| 行動方向 | 資料是否支持 | 結果 |
|---|---|---|
| 積極申請／布局 | ✓ | 輸出 |
| FTO／侵權風險調查 | ✓ | 輸出 |
| 迴避設計 | ✕ | 不輸出 |
| 專利收購／授權 | ? | 證據不足 |
| 持續監控競爭者 | ✓ | 輸出 |
| 防禦性布局 | ✕ | 不輸出 |

→ 自然得到 3 個行動。另一份資料支持 6 種就輸出 6 種，真的只有 1 種也允許。

🔴 **Verifier 檢查的不是「你有沒有寫 ≥4 個」，而是「所有候選方向你是不是都評估了」**：

- `covered = 8/8` → PASS（即使成立 2、否決 4、證據不足 2，最後只印 2 個行動）
- `covered = 1/8`（其餘 7 個連判斷都沒有）→ FAIL → Retry

**「固定 4 個行動」→「行動數量不限，但行動空間必須完整掃描」。**

⚠ 這個設計把**母體從「主題」換成「行動候選池」**，比我先前寫的「母體對帳保留」
更準確——主題數會變動、無法當固定基準，候選池是**固定且可窮舉**的，
`N/M` 才有意義。⚠ 它也真正解掉了矛盾：輸出可以只有 1 項（無數量下限），
但完整性仍被強制（候選池全掃），而且是集合比對、零語意判斷。

⚠ **FAIL → Retry 接得上既有機制**：`ai_report_deck_runner` 已有「check_content
未通過且 CLI 未修改 content.json → 停滯」的重試迴圈，不需新建。

### 9.1e 🔴 實查：引擎現在**真的產得出**哪些現象（2026-08-19，唯讀）

使用者：「現象用我們現在分析架構所能得出的現象來規劃。」以下是實查結果，
不是設想。

**引擎已有的五類技術狀態**（`cluster_analytics.classify_topic_status`，
判定優先序 衰退→競爭集中→成長→成熟→新興→未分類）：

| 狀態 | 引擎給的意思 | 判定依據（實際程式） |
|---|---|---|
| 新興 | 近三年才開始出現申請 | 量低 ∧ ratio ≥ 0.70 ∧ share_recent > share_early |
| 成長 | 件數與申請人數同步上升 | recent > early ∧ apps_recent > apps_early |
| 成熟 | 件數持平、方向趨於固定 | 量高 ∧ 0.59 ≤ ratio ≤ 0.79 |
| 競爭集中 | 申請持續但集中於少數人 | 量高 ∧ apps_recent < apps_early ∧ conc 上升 |
| 衰退 | 近年件數明顯下降 | recent < early ∧ share ↓ ∧ apps ↓ |
| 件數不足 | 判斷不可靠 | patent_count < 5（`STATUS_MIN_SAMPLE`） |

⚠ **狀態只給技術通道**（2026-08-03 使用者定案，功效通道不判演進狀態）
——行動空間掃描的母體因此是**技術主題**，不是全部主題。這條要寫進規格，
否則掃描範圍會被默認成「所有主題」而靜默縮小。

其他可用現象：象限四格（多方投入技術／低件數·多申請人／低件數·少申請人／
集中持有）、`max_share`／`top3_share`（集中度）、`early/recent` 件數與家數、
法律狀態分解（審查中／已授權／失效／未知，§7e 新增）、`leading_applicant_count`、
受理局與家族國家佈局、IPC/CPC 分布、外觀保護策略。

#### 9.1e-1 使用者行動表 A–G 對照實查結果

| R&D 行動 | 需要的現象 | 引擎有沒有 |
|---|---|---|
| A. 優先投入 | 新興＋成長 | ✅ 直接可判（`status`） |
| B. 加速深化 | 成長＋**已有自身基礎** | ⚠ 半套：成長 ✅／**自身基礎 ❌** |
| C. 差異化開發 | 成熟／密集＋集中 | ✅ （`status` ＋ `max_share`／象限） |
| D. 技術重組／跨域整合 | 空白／**相鄰技術出現連結** | ⚠ 半套：空白 ✅（象限「低件數·少申請人」）／**相鄰連結 ❌** |
| E. 探索性研發 | 新興但資料尚弱 | ✅ （`status` ＋ `patent_count < 5`） |
| F. 維持／漸進改善 | 成熟但仍重要 | ✅ （`status=成熟` ＋ 量高） |
| G. 降低投入／轉向 | 衰退或**被其他技術取代** | ⚠ 半套：衰退 ✅／**被取代 ❌** |

**四項可直接判（A／C／E／F），三項缺料（B／D／G）**，而缺的原因各不相同：

- **B 的「自身基礎」** ＝ 需要「本公司」一級概念。使用者 2026-08-18 已裁決
  「這兩輪不做，等模型改完再議」——**這是已知的、被刻意延後的能力缺口**。
- **D 的「相鄰技術連結」** ＝ 需要主題×主題的關聯度。引擎完全沒有這個計算
  （`model.py` 的 `cosine_similarity` 是**文件對主題**，不是主題對主題）。
  ⚠ c-TF-IDF 向量現成，理論上算得出來，但那是新功能不是接線。
- **G 的「被取代」** ＝ 需要跨主題的替代訊號（甲退乙進且技術手段相關）。
  同樣不存在，且比 D 更難：它要的是因果方向，不只是相關。

#### 9.1e-2 ⚠ 缺料的三項**不可以**塞進「證據不足」

「證據不足」的語意是**這份資料不夠**（換一份資料可能就判得出來）。
B／D／G 缺的是**系統根本沒有這個判準**——換再多資料也一樣。

兩者混在一起會製造一個很貴的誤解：使用者看到「證據不足」會去補資料，
補完發現還是證據不足，而真正的原因是引擎沒這個能力，且**沒有任何地方寫著**。

⚠ 這是「把缺席變成可見」的同一條原則，用在能力層而不是資料層。

- [x] 9.1e-3 🔴 **使用者裁決（2026-08-19）：候選池只放判得出來的**。
      B／D／G 缺的部分明文列進下方「已知未涵蓋」，不進池、不用假判定去填。

### 9.7 候選池定義（依 9.1e-3 裁決，只收判得出來的）

**入池條件：能寫出一條只用引擎欄位的判定規則。** 寫不出來就不進池。
🔴 這份清單是唯一定義處（比照 `LAYOUTS`／`ACTION_VERBS`）。

| # | 行動 | 判定規則（只用引擎欄位） | 來源現象 |
|---|---|---|---|
| 1 | 優先投入 | `status ∈ {新興, 成長}` | 五類狀態 |
| 2 | 差異化開發 | `status ∈ {成熟, 競爭集中}` ∨ 象限＝多方投入技術 | 五類狀態＋象限 |
| 3 | 探索性研發 | `status = 件數不足` ∨（`status = 新興` ∧ `patent_count < median`） | 五類狀態＋量 |
| 4 | 維持／漸進改善 | `status = 成熟` ∧ `patent_count ≥ median` | 五類狀態＋量 |
| 5 | 降低投入 | `status = 衰退` | 五類狀態 |
| 6 | 追蹤他人審查中案件 | `pending_count ≥ 1` | §7e 法律狀態分解 |
| 7 | 檢視請求項範圍重疊 | 象限＝多方投入技術 | `_qlabel` 現成 |
| 8 | 覆核代表專利 | 象限＝低件數·少申請人 | `_qlabel` 現成 |
| 9 | 確認權利集中程度 | 象限＝集中持有 ∨ `max_share` 高 | `_qlabel` 現成 |
| 10 | 評估利基切入 | 象限＝低件數·少申請人 ∧ `patent_count ≥ 1` | 象限（見 9.7d） |

#### 9.7d 🔴 「技術空白」改成「利基」（2026-08-19 使用者提議，採納）

原本 D「技術重組／跨域整合」需要「**空白**／相鄰技術出現連結」，因為兩項都缺
而整項不進池。使用者提議把「空白」換成「**利基**」——採納，而且理由不只是措辭：

| 用詞 | 主張的是什麼 | 我們的資料撐不撐得起 |
|---|---|---|
| 技術空白 | 「這裡**沒有人**做」＝缺席主張 | ❌ 母體只是**匯入的公開專利**。沒出現可能是沒人做、可能是檢索沒撈到、可能還沒公開。拿「沒看到」當「不存在」 |
| 利基 | 「這裡**有人做但很少**」＝在場主張 | ✅ 象限「低件數·少申請人」正是這個：`patent_count ≥ 1` 但量與家數都低 |

⚠ **但要再收一層**：資料只說「低密度」，說不出「有價值」。低密度也可能是
技術做不出來、或市場根本沒價值。所以沿用 `_qlabel` 的先例——
**現象用資料的話（低件數·少申請人），行動用查證動作（「評估」利基切入）**，
不寫成「這是個利基機會」那種結論。

⚠ #10 與 #8 觸發條件相同（同一象限）但用途不同：#8 是「去讀那幾件」，
#10 是「評估要不要投」。同一個現象觸發兩個不同層級的行動是合理的
——池子是**行動空間**不是分類，本來就允許重疊。

⚠ 這**不等於 D 回到池裡**：D 的「技術重組／跨域整合」還需要「相鄰技術連結」，
那項仍然缺（見 9.7a）。#10 是從 D 的前半獨立出來的一個新行動，不是 D 本身。

⚠ 7–9 **不是新發明**：`_qlabel` 現在就在回「（象限名, 後續行動）」兩元組，
四格各有一個建議動作。池子把它們納進來，而不是另立一套——否則就是第三份
各說各話的行動清單（§7a 剛修完同型問題）。

#### 9.7a 🔴 已知未涵蓋（明文記錄，不用假判定填）

| 使用者原表 | 缺什麼 | 為什麼不補 |
|---|---|---|
| B. 加速深化 | 「已有**自身**基礎」 | 需「本公司」一級概念，使用者 2026-08-18 裁決這兩輪不做 |
| D. 技術重組／跨域整合 | 「**相鄰技術**出現連結」 | 主題×主題關聯度，引擎沒有這個計算（是新功能不是接線）。⚠ D 的另一半「空白」已改成「利基」並獨立成 #10，見 9.7d——但那不等於 D 回池 |
| G 的後半 | 「被其他技術**取代**」 | 跨主題替代訊號，要因果方向不只相關；G 的前半（衰退）已入池為 #5 |

⚠ **不得**把這三項降級成「證據不足」混進掃描結果（理由見 9.1e-2）。
⚠ 這張表要出現在**規格與掃描檔**裡，不出現在報告裡（§9.6b）。

#### 9.7b 🔴 判定由**引擎**算，不是由 CLI 判（與專案核心原則一致）

9.7 表格右欄全部是確定性規則——那代表**引擎算得出來**，不需要 LLM 判斷。
這與 `.ai-rules/workflows.md` 的核心原則一致：
「引擎先產可信的結構化資料；AI 只**輔助**，不決定正式分類、不算正式統計」。

⚠ 這與使用者原本的框架（LLM 掃描 → Verifier 檢查 covered）不同，差別在
**誰做判定**。改由引擎判定的好處：確定性、可重現、零 token、
而且 `covered = M×K/M×K` **恆等成立**（引擎不會漏掃自己的迴圈），
Verifier 就不必去防 LLM 偷懶——那個風險從根上消失。

CLI 的工作改為：對引擎判成立的行動**寫判讀文字**（有自由度的部分）。

- [x] 9.7b-1 🔴 **使用者裁決（2026-08-19）：引擎算候選 ＋ CLI 可覆寫**。

      引擎用確定性規則算出候選判定（保證不漏掃）；CLI 可**加**規則沒觸發但
      資料支持的行動、可**否決**引擎判成立的，**兩種偏離都要附 `欄位=值` 引用
      並留痕，閘門不擋**（見 §9.8 的可反駁性原則）。

      ⚠ 擋了就變回天花板——規則沒涵蓋的真實機會永遠不會出現，那正是形式鎖
      的核心機制。⚠ 不留痕就等於沒發生過。
      ⚠ **偏離次數本身是規則品質的訊號**：同一條規則反覆被推翻，不是規則錯
      就是 CLI 在鑽同一個洞，兩者都看得出來。純規則設計沒有這個回饋迴路。
- [x] 9.7b-2 閘門改驗兩件事（比原設計簡單且更硬）：
      ① CLI 沒有捏造池外行動 ② 引擎判成立的行動，CLI 沒有漏寫判讀
      ⚠ ②才是真正防偷懶的那條，且它是集合比對、零語意

#### 9.7e 🔴 「交給機械就不會犯上世代的錯嗎」——不會，會犯**另一種**（使用者提問，2026-08-19）

**會。而且它就是形式鎖的同一族。**

規則寫死之後，**規則沒涵蓋的真實機會永遠不會出現，且不會有任何地方報錯**。
v5／v7／v9 的核心機制不是「鎖住格式」，是「用一條規則決定了什麼能存在」。
把判定交給引擎，等於把這個決定權從 LLM 移到規則——問題沒消失，只是換了位置。

兩種錯的差別在**可見度**，這才是改引擎判定的真正理由：

| | LLM 判定的錯 | 規則判定的錯 |
|---|---|---|
| 可重現 | ❌ 每次不同 | ✅ 每次一樣 |
| 找得到根因 | ❌ 散在生成過程 | ✅ 集中在一條規則 |
| 能被實測推翻 | ❌「這次剛好這樣」 | ✅ 拿反例跑一次就現形 |

**每次都一樣錯，是可以修的錯。** 但這只在有下面兩道護欄時才成立。

##### 9.7e-1 護欄一：規則是**地板不是天花板**（沿用本專案既有的候選模式）

公司正規化與主題候選都是這個模式：**AI／規則產候選，另一方可推翻但必須留痕**。

- [x] 9.7e-1a 引擎算**候選判定**（確定性、全覆蓋、零遺漏）
- [x] 9.7e-1b CLI 可**加**規則沒觸發但資料支持的行動 → **必須附證據錨點**
- [x] 9.7e-1c CLI 可**否決**引擎判成立的行動 → **必須附理由**
- [x] 9.7e-1d 兩種偏離都寫進掃描檔，**閘門不擋，但一定留痕**
      ⚠ 擋了就變回天花板；不留痕就等於沒發生過
- [x] 9.7e-1e **偏離次數本身是規則品質的訊號**：同一條規則每次都被推翻，
      代表規則錯了。純規則設計沒有這個回饋迴路，這是它最大的盲點

##### 9.7e-2 🔴 護欄二：門檻的來源必須先查證（**未做完不得在其上建行動池**）

`classify_topic_status` 的門檻是 **module-level 常數**，套用到每個 workspace，
而註解自己寫著它們是從**一份資料**切出來的：

| 常數 | 值 | 註解原文 |
|---|---|---|
| `STATUS_EARLY_YEARS` | (2011, 2019) | 「這三個數字是從實際資料切出來的，不是慣例」 |
| `STATUS_RECENT_YEARS` | (2020, 2024) | 同上 |
| `STATUS_GROWTH_HIGH` | 0.70 | 「全庫基準 R＝**38/55**＝0.69」←**55 ＝滑雪機 workspace 成員數** |
| `STATUS_MIN_SAMPLE` | 5 | 「**本案** 13 個主題有 3 個落在這裡」 |

⚠ 這是「把一份觀察當成常數」——與「憑印象挑回歸範圍」「28 種色」
「割草機 0 主題」同一種病。而行動池會**整個建在這個分類器上面**，
繼承它的每一個錯，且錯得很安靜。

⚠ 更明確的一顆定時炸彈：**年份窗是絕對值**。到 2027 年，「近期 2020–2024」
會變成三年前的資料，所有主題會一起被判成衰退——**沒有任何警報**。

- [x] 9.7e-2a 實測第二個 workspace（割草機）跑一次 `classify_topic_status`，
      看分類結果是否合理；⚠ 不是看有沒有跑完，是看**判出來的東西對不對**
- [x] 9.7e-2b ~~年份窗改為依母體推導~~ 🔴 **此修法已被 §9.9 取代**
      （2026-08-19 使用者：「workspace 是會變的，你也不能只用全庫的當依據」）。
      依母體推導同樣不穩：多匯入一批就可能讓同一主題從成長翻成衰退。
      正解見 9.9c 的三分法與 9.9d 的「判定必須帶基準」
- [x] 9.7e-2c 閘門：年份窗若落在未來或早於母體最早申請年即紅
      （防止常數過期而無聲）
- [x] 9.7e-2d 若 9.7e-2a 顯示分類器對第二個 workspace 不成立，
      **行動池要停下來等分類器修好**——建在錯的分類器上，
      掃描愈完整錯得愈整齊

### 9.8 🔴 CLI 的驗證原則與自驗機制（2026-08-19 使用者要求）

> 「應該要有一個驗證原則或機制讓 CLI 能遵守。」

9.7e-1 寫的「附證據錨點」「附理由」**對 CLI 是空的**——隨便寫都算有寫。
需要的是一條**它能拿來自我檢查**的判準，而不是一堆事後才擋的規則。

#### 9.8a 一條原則：**可反駁性**

> **每一句你寫的判斷，都要指得出「如果哪一個引擎欄位是別的值，這句話就不成立」。**

⚠ 這條原則不是新發明——它是本專案**已經證明有效**的那個機制的抽象化：
「發現欄逐字引用引擎字串」之所以守得住，正因為它的反駁條件是機械的
（字串不符即紅）。把同一個形狀推廣到所有判斷。

給 CLI 的操作句：
**不要說「規則錯了」，要說「規則沒看 `X` 欄位，而這個主題 `X = Y`」。**

#### 9.8b 三個可機械驗的形式

| CLI 要做的事 | 必須附上 | 閘門怎麼驗（零語意判斷） |
|---|---|---|
| 引用數據 | 逐字引擎字串 | 字串比對（§7 現行作法，已上線） |
| 寫判讀 | ≥1 個 `欄位=值` 引用 | 欄位在該主題的引擎輸出裡存在，且值逐字相符 |
| **加**行動（規則沒觸發） | `欄位=值` ＋ 指出規則沒看這個欄位 | 同上 |
| **否決**行動（規則觸發了） | `欄位=值` ＋ 指出規則看錯哪裡 | 同上 |

- [x] 9.8b-1 引用格式定成單一形式（例如 `[status=成長]`），**唯一定義處**，
      三個場合共用——不要三種寫法
- [x] 9.8b-2 閘門：引用的欄位必須在 `topic_facts`／掃描檔裡存在，值逐字相符
- [x] 9.8b-3 Red：引用一個不存在的欄位 → 紅；引用存在的欄位但值寫錯 → 紅
- [x] 9.8b-4 ⚠ 引用字串屬**內部語言**，渲染前要移除或轉成讀者語言
      （同 §9.6b：內部判定不外洩）

#### 9.8c 自驗迴圈：CLI 交稿前自己先跑一次

現況：CLI 寫完 → runner 跑 `check_content` → 紅了把 `gate_output` 打回去改。
**CLI 從來沒有在交稿前自己驗過。**

- [x] 9.8c-1 撰稿提示明確要求：交稿前自行執行 `check_content.py`，紅了自己修
      ⚠ 開發側 SKILL.md L367 早就有這條指令，只是產品側沒要求 CLI 用
- [x] 9.8c-2 ⚠ **不移除** runner 那道閘門——自驗是省往返，不是取代把關。
      「它說它驗過了」不是證據
- [x] 9.8c-3 自驗與 runner 驗**必須是同一支腳本**，不得為了自驗另寫一份精簡版
      （兩份必然分岔，且分岔時 CLI 那份會比較鬆）

#### 9.8d ⚠ 這套原則自己先過三問（design §1.2）

| 問 | 判定 |
|---|---|
| ① 不看語意能否判定？ | **過**。欄位存在 ＋ 值逐字相符，純比對 |
| ② 滿足它的**唯一**途徑＝做對？ | **不過（同 9.5 的洞）** |
| ③ 偏差是多出來的還是缺席的？ | 引用造假＝多出來的（可見）；該寫沒寫＝缺席，靠引擎地板兜住 |

⚠ **第二問的洞**：CLI 可以引用一個**真實存在**的欄位值，而那個值跟它的主張
毫無關係。這套機制驗的是**形狀**，不是**推理**。

⚠ 但它比 9.5 的證據錨點多一層防護：因為引擎**已經算過一個判定**，
CLI 的偏離是「與某一條具名規則不同意」，這在**累積**上是可稽核的
——同一條規則反覆被同一種引用推翻，不是規則錯就是 CLI 在鑽同一個洞，
兩者都看得出來。⚠ 單次仍然守不住，這點必須明講，不得宣稱閘門擋得住亂寫。

- [x] 9.8d-1 掃描檔保留每次的偏離紀錄（規則、引用、理由），
      讓「累積可稽核」成立而不只是說法

### 9.9 🔴 判準的基準問題（2026-08-19 使用者指出，**取代 9.7e-2b**）

> 「分類器在這環節之前就要依據建立在 workspace 的資料才有意義，這應該去掃程式。」
> 「workspace 是會變的，你也不能只用全庫的當依據。」

#### 9.9a 掃描結果（唯讀，兩遍）

腳本：`audit_thresholds.py`（模組層具名常數）＋`audit_inline_thresholds.py`
（函式內與資料比較的裸數字）。⚠ 只掃第一遍會漏掉沒有名字的那一半。

**第一遍**：分析路徑下模組層數值常數 **50 個**，其中 **4 個註解自承出自單一資料集**：

| 檔:行 | 常數 | 註解原文（節錄） |
|---|---|---|
| `cluster_analytics.py:46` | `STATUS_EARLY_YEARS = (2011, 2019)` | 「這三個數字是從**實際資料**切出來的，不是慣例」 |
| `cluster_analytics.py:51` | `STATUS_GROWTH_HIGH = 0.70` | 「**全庫基準 R＝38/55**」←55＝滑雪機成員數 |
| `cluster_analytics.py:56` | `STATUS_MIN_SAMPLE = 5` | 「**本案** 13 個主題有 3 個落在這裡」 |
| `clustering/runner.py:60` | `MIN_CLUSTERING_DOCUMENTS = 30` | 「實機動因：**滑雪機** 60 筆專利…」 |

⚠ `STATUS_RECENT_YEARS = (2020, 2024)` 沒被標記只因為註解寫在上一個常數頭上
——它是同一組。**訊號式掃描會漏這種**，掃描結果要人看過，不能只看紅字數量。

**第二遍**：函式內與資料比較的裸數字 **16 處**，多數是結構檢查
（`len(vector) != 768`、`== 2`），真正的資料門檻 **3 處**：

| 檔:行 | 判準 | 性質 |
|---|---|---|
| `chart_runner.py:4659` | `leading_count >= 2` | 龍頭涉入三級的分界 |
| `clustering/runner.py:367` | `document_count < 50` | 分群 k 值的規模分界 |
| `clustering/runner.py:369` | `document_count < 100` | 同上 |

#### 9.9b 🔴 使用者的第二句推翻了我原本的修法

我在 9.7e-2b 寫「年份窗改為**依母體推導**」——**那個修法不成立**。

`workspace 會變`：同一個 workspace 多匯入一批專利，母體變、門檻跟著變，
於是**同一個主題可能從「成長」翻成「衰退」，而技術本身什麼都沒發生**。
`全庫也會變`：全庫是所有 workspace 的和，變得更頻繁。

**兩個候選基準都不穩。** 這不是選哪一個的問題，是「有沒有絕對基準」的問題
——沒有。

#### 9.9c 三分法：數字該固定還是該浮動，看它的**依據**不是看它的位置

| 類別 | 例子 | 該怎麼辦 |
|---|---|---|
| **制度事實** | 專利公開延遲約 18 個月、專利年限 20 年 | **固定**，但必須註明依據（可外部查證） |
| **母體相對** | 中位數、分位數、占比 | **per-run 推導**，且**必須把基準值一起輸出** |
| **從一份觀察抓的** | `0.70`（38/55）、`5`、`(2011,2019)` | 🔴 兩者皆非，**要改** |

⚠ 現行年份窗其實**混了兩種**：「排除最近兩年（資料截止效應）」是**制度事實**
（新案未公開），那部分是對的；錯的是把它的邊界寫成絕對年份 `2025`。
⚠ 拆開之後，年份窗應該是「**母體最新申請年往前推 N 年**」，
其中 N 有制度依據（公開延遲），而起點跟著資料走。

#### 9.9d 判定必須**帶著它的基準**，否則跨批次不可比

母體相對的判定不是技術的**屬性**，是「**相對於本批母體**」的位置。
現在的輸出（`status = 成長`）讀起來像屬性，這是誤導的來源。

- [x] 9.9d-1 判定連同基準一起輸出（例如 `status=成長, 基準={中位數:8, 近期窗:2020-2024}`）
- [x] 9.9d-2 ⚠ 基準值屬**內部語言**，不直接印給讀者（§9.6b）；
      但要進掃描檔，讓「為什麼這批跟上批判得不一樣」查得到
- [x] 9.9d-3 報告措辭改成相對句（「在本批資料中屬成長」），不寫成絕對屬性

#### 9.9e 🔴 判準的驗證方式（2026-08-19 使用者修正，**取代我原本的翻轉率實測**）

> 「不用加資料重跑，你只要確定每次做報告不會是固定某 workspace，
> 所以不同 workspace 不能混到（全庫除外），資料也是會有變化的，
> 所以機制不能是寫死的。」

⚠ 我原本要做的是「加一批資料量判定翻轉率」——**方向錯了**。翻轉率量的是
「結果穩不穩」，但結果本來就該隨資料變；真正該驗的是**基準的來源對不對**。

**判準：每個門檻的基準母體，必須等於本次報告的母體。**

| 情形 | 對不對 |
|---|---|
| 門檻由**本次報告母體**算出 | ✅ |
| 門檻由**全庫**算出，而報告就是全庫報告 | ✅（使用者明示的例外） |
| 門檻由**全庫**算出，但報告是某 workspace | ❌ 別的 workspace 混進來了 |
| 門檻是**寫死的數字** | ❌ 那是**產生它的那一份資料**混進來了 |

🔴 **這與第 1 段的母體閘門是同一種病**：`STATUS_GROWTH_HIGH = 0.70` 出自
滑雪機（38/55），套到割草機上，**就是不同 workspace 的資料混進來**
——只是混進來的是門檻，不是件數。差別只在它偽裝成常數。

- [x] 9.9e-1 閘門與 §1 的 `backend/app/db/population_scope.py` **同源**，
      不另建第二份掃描邏輯（⚠ 同一份知識一個定義處；兩份掃描器會分岔，
      而分岔時比較鬆的那份會被拿來當「已經檢查過了」）
- [x] 9.9e-2 判準：分析路徑下**與資料比較的數值門檻**，其基準必須追得到
      本次母體；追不到即紅
- [x] 9.9e-3 例外只有一種——**制度事實**（公開延遲、專利年限），
      且必須在原地註明依據（可外部查證）。⚠ 例外要進白名單並附理由，
      比照 §1.3 的 `POPULATION_SCOPE_EXEMPT`：**空理由不算**
- [x] 9.9e-4 Red：把某個 per-run 推導的門檻改回寫死常數 → 要紅
- [x] 9.9e-5 Red：把某個門檻的基準從 `patent_ids` 換成全庫 → 要紅
      ⚠ 這條才是「不同 workspace 不能混到」的機械形式

##### 9.9e-6 ⚠「機制不能是寫死的」的兩種違反，第二種比較難看見

1. **值寫死**：`STATUS_GROWTH_HIGH = 0.70`——一眼看得出來。
2. **機制寫死**：判定式本身假設了某種資料形狀。例如
   `STATUS_STAGNANT_BAND = (0.59, 0.79)` 假設「近期占比」這個量是有意義的，
   而那只在「早期窗與近期窗長度相近」時成立；滑雪機是 9 年 vs 5 年，
   換一批資料年份分布完全不同時，同一個 ratio 代表的意思就不一樣了。
   ⚠ 把 0.59/0.79 改成 per-run 推導**不會**修好這個——修的是值，機制還是寫死的。

- [x] 9.9e-7 逐條檢查每個判定式：它假設了資料的什麼形狀？該假設在別的
      workspace 成立嗎？⚠ 這一項**無法機械化**，是人工判讀；
      但要逐條寫下來，不寫下來就會被當成沒有假設

#### 9.9f 三個一定要一起改，否則只是換個地方寫死

- [x] 9.9f-1 `STATUS_*` 四個常數（`cluster_analytics.py:46/47/51/56`）
- [x] 9.9f-2 `MIN_CLUSTERING_DOCUMENTS`（`clustering/runner.py:60`）
- [x] 9.9f-3 `leading_count >= 2`、`document_count < 50/100` 三處裸門檻先具名，
      具名之後才看得出它們是哪一類（現在連討論的對象都沒有名字）
- [x] 9.9f-4 掃描腳本落地成 `scripts/audit_thresholds.py`（唯讀），
      比照 `audit_population_scope.py`；⚠ 兩遍要在同一支，不然下次又只跑一半

#### 9.9g ⚠ 9.9e 這道閘門自己先過三問（使用者：「不要犯到上世代的錯」）

| 問 | 判定 |
|---|---|
| ① 不看語意能否判定？ | **半過**。「是不是字面常數」機械可判；「基準是否來自本次母體」要追資料流，實務上會退化成前者 |
| ② 滿足它的**唯一**途徑＝做對？ | **不過（重大洞，見下）** |
| ③ 偏差是多出來的還是缺席的？ | 白名單會長大（缺席型）；9.9e-7 的人工判讀不寫就等於沒有 |

##### 🔴 第二問的洞：**把常數搬家就能過關**

閘門實際上只驗得了「原始碼裡沒有字面數值門檻」。要滿足它，最省力的做法**不是**
把基準改成母體推導，而是：

- 把 `0.70` 搬進設定檔／環境變數／DB 一列 → 掃描器看不到字面值，**閘門綠**，
  **行為完全沒變**
- 或改寫成 `threshold = 7 / 10` → 連字面值都算不出來
- 或 `threshold = len(rows) * 0.07` → per-run、非字面，但基準毫無意義

⚠ 這正是 v5／v7／v9 形式鎖的死法重演：**為了過鎖而搬家，而不是把事情做對**。
而且這次更糟——搬家之後，那個數字從「有註解說明它出自滑雪機」變成
「設定檔裡一個沒有來歷的值」，**可追溯性反而下降**。

- [x] 9.9g-1 閘門判準改成**正面表述**：每個判準必須宣告它的
      `基準來源 ∈ {本次母體, 制度事實, 全庫(僅全庫報表)}`，**沒有宣告即紅**。
      ⚠ 這比「不得出現字面常數」硬：搬家不會產生宣告，改寫算式也不會
- [x] 9.9g-2 `制度事實` 的宣告必須附**可外部查證的依據**（法規／公開延遲月數），
      理由非空且不得是「沿用既有」「實測如此」這類自我指涉
      （比照 §1.3 白名單的空理由不算）
- [x] 9.9g-3 白名單凍結：比照 §1 的 `REVIEWED_EXEMPTIONS`，
      已審過的例外寫成明確集合，**新增例外必須改測試**才能通過
      ——不改測試就悄悄多一個例外，是 §1 踩過的同型
- [x] 9.9g-4 反向驗證要涵蓋**搬家**這個手法：把常數移到設定檔 → 仍要紅
      ⚠ 只驗「改回字面常數會紅」不夠，那不是真正的規避路徑

##### 9.9g-5 ⚠ 誠實揭露這道閘門守不住的部分

宣告制可以擋「沒說」，擋不了「說謊」——宣告 `本次母體` 但實際算錯母體，
閘門看不出來。那一層靠 §1 的母體閘門與實測數字對帳
（兩個 workspace 數字不相等且各自正確）。

⚠ 兩道閘門**必須都在**：§1 驗「統計的母體對不對」，9.9 驗「門檻的母體對不對」。
少一道，另一道就會被拿來當「已經檢查過了」。

#### 9.7c 呈現沿用 §7f 的行動分組（不新設版面）

引擎逐主題判定 → 依行動分組 → 每組列出成立的主題。
這正是 §7f `conclusion_groups` 現在的行為（只畫真的用到的分組）。
使用者範例「最後自然得到 3 個行動」＝有 3 個行動至少一個主題成立 → 畫 3 組。

⚠ 因此 9.1c-1（全域 vs 逐主題）**自動有解**：判定逐主題、呈現依行動分組，
兩種讀法都成立，不必二選一。
⚠ 9.1c-2（與 `ACTION_VERBS` 的關係）**變成取代**：9.7 這份池子就是新的
`ACTION_VERBS`，舊的五個動詞併入或退場——不留兩份。

#### 9.1e-4 既有先例：`_qlabel` 已經處理過同一個問題

`chart_runner._qlabel` 的 docstring：

> 2026-08-02：高密度高廣度原本叫「必守核心戰場 → **迴避設計**」。
> ⚠ 這張圖只有「件數 × 申請人家數」兩個維度，**推不出迴避設計結論**——
> 真正的 FTO 需要 claim chart、claim overlap、legal status、jurisdiction，
> 一項都不在這裡。用密度統計冒充侵權判斷會誤導決策，故改為描述**現象**
> （高競爭技術區）與**下一步查證動作**（claim overlap 分析）。

🔴 **這條先例直接適用於行動池**：行動必須落在「**下一步查證動作**」的層級，
不能落在「法律／商業結論」的層級。它同時也是 9.1c-3 那個衝突的現成解法
——「FTO／侵權風險調查」寫成**查證動作**是成立的，寫成結論才不成立。

#### 9.1c 三個實作前必須解決的衝突（**都需使用者裁決**）

- [x] 9.1c-1 **候選池是全域還是逐主題？**
      使用者範例讀起來是**全域**（一份分析得到 3 個行動）。但現行 `conclusions`
      是**一主題一列、每列一個 `action`**，且 §7f 才剛裁決「一列可多個行動」。
      兩種模型的資料形狀完全不同：
      (甲) 全域——第 2 頁改成「行動 × 判定 × 證據」，主題退居證據欄
      (乙) 逐主題——每個主題各掃一次候選池，`covered` 變成 M×K 格
      (丙) 兩層——全域行動頁 ＋ 既有逐主題結論頁並存（⚠ 但這違反「同一問題
           不答兩次」，要有明確分工才成立）

- [x] 9.1c-2 **新候選池與既有 `ACTION_VERBS` 是什麼關係？**
      現行 `ACTION_VERBS = ("佈局", "追蹤", "迴避設計", "細讀比對", "暫不投入")`
      是**逐主題**動詞；使用者列的六項（積極申請／布局、FTO／侵權風險調查、
      迴避設計、專利收購／授權、持續監控競爭者、防禦性布局）是**公司層策略**，
      粒度不同。取代？並存？合併成一份？
      ⚠ 不論選哪個，**候選池必須是唯一定義處**（比照 `LAYOUTS`／`ACTION_VERBS`），
      否則會變成第三份各說各話的清單（§7a 剛修完同型問題）。

- [x] 9.1c-3 🔴 **候選池與既有法律護欄衝突**
      `deck_layout.ACTION_VERBS` 的註解明訂：動詞白名單存在的理由是
      「避免 AI 寫出**『應立即提出申請』**這類法律／商業承諾」。
      而使用者候選池裡有「**積極申請／布局**」；簡報頁尾又固定聲明
      「本分析**非**侵權迴避(FTO)結論」，候選池裡卻有「**FTO／侵權風險調查**」。
      ⚠ 我的判斷是兩者**可以並存但必須寫明分界**：建議「去做 FTO 調查」
      不等於「給出 FTO 結論」；建議「布局方向」不等於「應立即提出申請」。
      分界沒寫明的話，下一個人會直接把護欄放寬——那是靜默的合規風險。

#### 9.1d ⚠ 這個設計自己的兩個已知邊界（寫下來，不假裝沒有）

1. **候選池的完整性本身無法機械驗證**。`covered 8/8` 只證明「池子裡的 8 個都
   評估了」，不證明「池子裡本來就該有 8 個」。池子漏了一個方向，掃描會
   **完整地掃錯**。⚠ 這不是缺陷，是判斷的落點——但必須明寫，
   否則 `8/8` 的綠燈會被讀成「窮盡了所有可能」。
2. **「證據不足」是偷懶的出口**。全部標成證據不足，`covered 8/8` 照樣 PASS。
   ⚠ **不加**「證據不足不得超過 N 個」的鎖（又是數量鎖）。改為要求每個
   證據不足**寫出「還缺什麼才能判定」**——讓偷懶變貴且可見，不禁止它。

### 9.2 🔴 rec 退場的前置條件（**未解決前不得退場**）

`_compose` 是 `if content.get("conclusions"): slide_conclusions else: slide_rec`，
而 `_check_conclusions` **只有在引擎產出主題時才要求 conclusions**（`facts` 空即放行）。

- 有分群主題（滑雪機）→ conclusions，rec 不畫
- **無分群主題（割草機 0 主題，見 7e.3）→ rec 是唯一的第 2 頁**

直接退場會讓那類 workspace **整個第 2 頁消失**——缺席型偏差，而且是最貴的那種
（使用者拿到的簡報少一頁而沒有任何訊息）。

- [x] 9.2a 🔴 **前置條件解除**（2026-08-19 使用者：「沒有分群不會去進入做簡報」）。

      我原本列的兩條路（conclusions 增非主題模式／rec 保留為退路）**都不需要**
      ——deck 本來就只在有分群的情況下產出，所以「無主題的 workspace」不是
      deck 會遇到的情境。

      ⚠ **但實查：程式裡沒有這道守門**。`_compose` 是
      `if conclusions else slide_rec`，`_check_conclusions` 也只在 `facts`
      非空時才要求 conclusions；沒有任何一處拒絕「無主題就產 deck」。
      也就是說那是**流程上不會**，不是**程式擋著**。

      這個差別決定 rec 退場安不安全：退場後若真有人在無分群時跑 deck，
      第 2 頁會**靜默消失**（缺席型偏差，而且是最貴的那種——使用者拿到的
      簡報少一頁而沒有任何訊息）。

- [x] 9.2c 把流程假設寫成**明確前置檢查**：deck runner 在 assemble 後確認
      `topic_facts.json` 非空，空的就**明確失敗並說原因**
      （「本版本沒有分群主題，無法產簡報；請先完成分群」），
      不是少畫一頁。⚠ 這是 rec 能安全退場的前提
- [x] 9.2d Red：給一份無主題的 report_data → runner 要明確失敗且訊息可讀
      ⚠ 判準是「訊息說得出原因」，不是「有 raise」——只 raise 的話
      使用者看到的是一個技術例外，仍然不知道要去做分群

### 9.3 拆數量鎖

- [x] 9.3.1 移除 `check_content.py:428` 的 `not in (3, 4, 5)`
- [x] 9.3.2 同步移除 `SKILL.md` 的「用 4 張（2×2）」與 `content-template.json`
      的四張範例（範例列數 CLI 會照抄——§7 已證實）
- [x] 9.3.3 `narrative.md:87` 的「四類研發方向」範例改成不綁數字的寫法。
      ⚠ 但 L89 那條「標題裡的量詞必須與下方項目數一致」**要留著**——
      它管的是一致性不是數量，正是數量浮動後更需要的

### 9.4 自動分頁（版面）

- [x] 9.4.1 依實測高度切頁，續頁標題帶「（續）」
      ⚠ 高度**只能量不能估**（`text_h`，同結論頁既有作法）
- [x] 9.4.2 頁碼佔位符（`{chart_first}`／`{last}`／`{total}`）必須跟著算對
      ——分頁會改變總頁數，寫死的頁碼是既有踩過的坑
- [x] 9.4.3 Red：給 N 項（N 大到必然跨頁）→ 產出頁數 > 1 且**沒有任何一項遺失**
      ⚠ 判準是「項目集合相等」，不是「頁數等於 ceil(N/k)」——後者把版面演算法
      寫進測試，改行距就假紅

### 9.5 證據鏈（使用者第 3 題的裁決）

現行結論列＝`{topic, finding, reading, action}`：
`finding`＝分析結果（引擎，逐字閘門）、`reading`＝結論、`action`＝專利行動。
**四段裡缺「證據」那一段**。

- [x] 9.5.1 結論列新增 `evidence` 欄，內容必須是**可反查的錨點**
      （專利號／`patent_ids`／主題碼），不是自由敘述
      ⚠ 沿用 deepen 3.1：`plan.json` 的 `claim_lookup.players[].patent_ids` 已現成
- [x] 9.5.2 閘門：`evidence` 裡的每個錨點都要在引擎輸出裡真的存在，
      對不上即紅（＝「反向追溯」的機械形式）
- [x] 9.5.3 Red：塞一個不存在的專利號進 evidence → 要紅
- [x] 9.5.4 ❌ **不驗**「證據足不足以支持該結論」——那是語意判斷，有自由度，
      屬輪二。本節只驗鏈**接不接得上**，不驗鏈**夠不夠強**

### 9.6 行動空間掃描的閘門（實作 9.1b）

- [x] 9.6.1 候選池唯一定義處（比照 `LAYOUTS`／`ACTION_VERBS`），每項帶
      **語意說明**——CLI 要判斷「這個方向成不成立」，沒有說明就只能望文生義
- [x] 9.6.2 content 新增行動掃描區塊：每個候選項一筆
      `{action, verdict: 成立|不成立|證據不足, evidence, reason}`
- [x] 9.6.3 閘門：掃描區塊的 `action` 集合**必須等於**候選池
      （少一個即紅、多一個也紅——多的代表 CLI 自創了池外方向）
- [x] 9.6.4 閘門：`verdict` 只能是三值之一；`成立` 必須帶 evidence 錨點
      （接 9.5.2）；`不成立`／`證據不足` 必須帶 `reason`
- [x] 9.6.5 閘門：`證據不足` 的 `reason` 必須寫出「**還缺什麼才能判定**」
      ⚠ 這是 9.1d-2 的對策：讓偷懶變貴且可見，**不設數量上限**
### 9.6b 🔴 判定措辭是**系統內部語言**，不得外洩到報告（2026-08-19 使用者裁決）

> 「成立／不成立／證據不足這種事系統內部判定，不要讓這種措辭外洩到報告去。」

⚠ 這條**取代**我原先寫的 9.6.6（「不成立／證據不足進附錄或摺疊區」）——
那等於把內部判定語言直接印給讀者看。使用者看到的應該是**結論**，
不是系統的推導狀態。

⚠ 同型問題本專案已經處理過一次：deepen 4.1／4.2 把 `read_me`／`chart_rule`
從封面移除、並對「本簡報怎麼讀」「圖表原則」「**待驗證**」「**降級**」建黑名單
——理由完全一樣：那是流程狀態，不是分析結論。「證據不足」與「降級」是同一類詞。

- [x] 9.6b-1 掃描結果放 content.json 的**非渲染鍵**（比照既有 `_說明`／`_pages說明`
      的慣例），閘門讀得到、版面畫不到。⚠ 不可放進任何會被 `slide_*` 取用的欄位
- [x] 9.6b-2 洩漏閘門：`成立`／`不成立`／`證據不足`／`covered`／`N/M` 這類判定與
      對帳措辭，**不得出現在任何會被渲染的文字**（沿用 deepen 4.2 的有限黑名單
      作法，不做模式比對）
- [x] 9.6b-3 Red：把「證據不足」寫進某頁 `lines` → 要紅
- [x] 9.6b-4 反向驗證：黑名單清空 → 9.6b-3 要變綠（證明是黑名單在擋，
      不是別的規則順手擋掉的）

#### 9.6b-5 ⚠ 但「掃過」這件事必須留得下證據

掃了卻完全不顯示，使用者無從判斷掃得對不對——這是我原本寫 9.6.6 的理由，
理由仍然成立，只是**落點錯了**。修正後的分工：

| 給誰看 | 放哪 | 內容 |
|---|---|---|
| 閘門與稽核 | 工作目錄的掃描檔（非渲染） | 全部候選、判定、證據錨點、理由 |
| 報告讀者 | 投影片 | **只有成立的行動**，用結論語言 |

- [x] 9.6b-6 掃描檔隨產出一併保存（比照 `topic_facts.json`／`cover_stats.json`），
      使用者要查「為什麼沒建議 X」時打得開
- [x] 9.6b-7 ⚠ 若日後決定要在報告裡交代「哪些方向看過但沒建議」，
      **措辭必須另行設計**（讀者語言，例如「本次資料未支持」），
      不得直接搬內部判定值——這條先記著，本輪不做
- [x] 9.6.7 Red：漏掉一個候選方向 → `covered 7/8` → 紅
- [x] 9.6.8 Red：全部標 `證據不足` 但都沒寫「還缺什麼」→ 紅
      （驗 9.6.5 真的有牙齒，不是宣示）
- [x] 9.6.9 反向驗證：把 `covered` 對帳字串改成硬編碼 `8/8` → 必須紅
      ⚠ 對帳數字若可由 CLI 自填而不被核對，這整套就只是裝飾

#### 9.5 這道閘門先過三問（design §1.2）

| 問 | 判定 |
|---|---|
| ① 不看語意能否判定？ | **過**。錨點在不在引擎輸出裡，是集合比對 |
| ② 滿足它的**唯一**途徑是否＝把事情做對？ | **不過（有洞，見下）** |
| ③ 偏差是多出來的還是缺席的？ | 錨點造假＝多出來的（可見）；但**整項不寫**＝缺席 |

⚠ **第二問的洞**：CLI 可以隨便挑一個**真的存在**的專利號填進 evidence 來過關
——錨點存在不等於它支持那個結論。這就是為什麼 9.5.4 明講不驗強度：
承認這個洞、把它留給人看，比假裝閘門守得住好。
⚠ 也正因為有這個洞，**9.1 第 3 點的證據鏈不能取代母體對帳**（見 9.1 解讀）：
對帳管缺席，是這道閘門結構上管不到的方向。

## 7. 版型庫收斂（2026-08-18 新增）

⚠ 全部零自由度——只驗「有沒有、幾個」，不驗好壞。適用情境那類有自由度的，
一律留在輪二。

### 7a. 三份清單各說各話（根因）

實查：能畫的（`deck_layout`）、會擋的（`check_content`）、CLI 照抄的
（`content-template.json`）互不一致。`conclusions` 有畫法、有閘門，
**範本裡沒有**——CLI 不宣告就 `if not cc: return []` 靜默放行，那頁根本不產出。

- [x] 7a.1 Red：版型清單唯一定義處 `deck_layout.LAYOUTS`（比照 `ACTION_VERBS`）
- [x] 7a.2 Red：**三處同步閘門**——`check_content` 認得的、範本示範的、
      `narrative.md` 寫到的，三者集合必須等於 `LAYOUTS`；任一缺漏即紅
- [x] 7a.3 反向驗證：`LAYOUTS` 加一個不在範本裡的版型 → 閘門要紅
      ⚠ 這條會**當場抓出還有哪些版型漏登記**，不要跳過
- [x] 7a.4 `conclusions` 補進 `content-template.json`

### 7b. 結論頁閘門的三個破口

現況 `_check_conclusions` 的三行：

```python
if not cc: return []      # ① 沒宣告就放行
if not rows: ...          # ② 只驗非空——10 主題寫 1 列全綠
if topic in facts: ...    # ③ 主題名不在 facts 就整個跳過逐字比對
```

- [x] 7b.1 Red：`conclusions` 缺席時要紅（頁是必要的，不是可選的）
- [x] 7b.2 Red：主題名不在 `topic_facts` 時要紅（不得靜默略過比對）
- [x] 7b.3 Red：**涵蓋率對帳**——`conclusions` 要帶 `covered N/M` 與
      未涵蓋主題的逐條原因；缺對帳即紅
      ⚠ **不是最小列數**。規定列數是形式鎖（v5／v7／v9 三次的同型錯），
      會逼出硬湊；涵蓋率只要求「沒寫的要現形」，偏差從缺席變成可見清單
- [x] 7b.4 反向驗證：把某主題從結論刪掉但不更新對帳 → 要紅

### 7c. 表格通用版型

表格繪製**已經會了**（`slide_conclusions` 的四欄表、`CONCL_COLS` 欄寬），
只是綁死在那一頁。

- [x] 7c.1 抽成 `layout: "table"`：**參數化欄數與欄寬**（2026-08-18 使用者裁決）
      欄寬由版型依欄數計算，總和不得超過可用寬（沿用 `CONCL_COLS` 的內距紀律）
- [x] 7c.2 Red：欄數與資料列的欄位數不符即紅；欄寬總和超寬即紅
- [x] 7c.3 `slide_conclusions` 改用通用表格版型，不留第二份畫法

### 7d. 路線圖頁併入結論頁

2026-08-18 使用者裁決：**期程欄整個拿掉**；路線圖頁與結論頁合併成一頁。

⚠ 期程（`短期 0–3 個月` 等）是全份唯一沒有資料支撐的欄位——系統不知道人力、
預算與產品排程，那個數字必然是編的。

- [x] 7d.1 移除 `content-template.json` 的 `roadmap*` 區塊與三期程
- [x] 7d.2 結論頁改為**依 `ACTION_VERBS` 分組**呈現（同一批 rows，換分組）
- [x] 7d.3 排序依**外部訊號**：該主題的他人審查中件數（多者在前）
      ⚠ 這是對手給的時間壓力，有資料可查證；不是我們假設的月份
- [x] 7d.4 Red：`roadmap` 仍出現在 content 即紅（避免舊範本殘留）

### 7f. 專利行動改為可多選（2026-08-19 使用者裁決，規格回寫）

使用者：「一種策略也許適用多個主題，也可能只適用一個主題……但也不是說行動就
只能選擇一種而已。」前半（一動詞對多主題）7d.2 的分組本來就成立；後半不成立
——`action` 是單一字串，等於強迫一主題一行動。「先迴避設計、同時追蹤對手審查
中的案」寫不出來就得二選一，而被丟掉的那個**不留任何痕跡**（缺席型偏差）。

- [x] 7f.1 `deck_layout.row_actions()`＝解析唯一定義處（字串／list 都收）；
      `check_content._bad_actions()` 讀同一份——多值最容易漏「只驗第一個」
- [x] 7f.2 該列在每個宣告動詞的分組下各出現一次；行動格印本分組動詞，
      其餘掛「同時：X」灰小字，避免讀者以為分組錯了
- [x] 7f.3 ⚠ **不加**「至多／至少 N 種」數量鎖（v5／v7／v9 形式鎖同型）。
      約束留在既有那條：每個動詞都要能從同列「判讀」推得出來
- [x] 7f.4 半真機械鏈實跑到多動詞列（`test_deck_runner_semireal` 代打 CLI）

### 7g. 兩個只有真產 pptx 才會炸的錯（半真鏈抓到，2026-08-19）

⚠ 兩個都是「同一份知識兩個落點」，而且**deck 側單元測試全綠**——
SVG 路徑與 PPTX 路徑對同一份輸入不同意，沒有人對帳。

- [x] 7g.1 `{"size": 11}` 裸 int：SVG 讀 pt 數值正常，PPTX 當成 11 EMU
      → 0 centipoints 直接 ValueError。修在 `_set_font`（唯一落點）。
      ⚠ 判斷用 `Length` 不是 `int`——`Pt(16)` 是 int 的子類別，第一版修法
      把既有 `B_SIZE` 二次換算，同一個 ValueError 換個數字
- [x] 7g.2 字級白名單兩份（`deck_layout` 定 24/16、`audit_deck` 寫死
      `"24,16"`）→ 收斂成 `ALLOWED_SIZES`，audit 預設讀它；
      新增 `S_SIZE = Pt(11)` 註記小字層級（不是「塞不下就縮字」的後門）

### 7e. 引擎供給外部訊號

- [x] 7e.1 `cluster_topic_table` 加法律狀態分解（`_status_breakdown`）
      ⚠ 走 `mappings/legal_status` 唯一定義處，不在此重判
- [x] 7e.2 不新增查詢——法律狀態併進 loader 既有的申請人查詢（loader 註解明訂 patents 是單一入口）；範圍沿用 cluster 的 workspace scope
- [x] 7e.3 ⚠ **無法用兩 workspace 比對**：只有滑雪機有分群資料（割草機 0 主題）。改以「合計 == 分群母體」與「每主題合計 == 該主題件數」兩條驗
- [x] 7e.4 🔴 **合計對上 cluster 母體**——實測合計 44、成員 55，未把排除的算進來
      實測滑雪機：workspace 成員 55、cluster 指派 **44**（11 件外觀設計分不了群）。
      分解件數合計要等於 **44**；寫成 55 就是把「刻意排除」偽裝成「全都算到了」，
      那才是真的新種一個同型錯。
- [x] 7e.5 主題分析 section 的 note 帶母體字串（分群 44／workspace 55，並說明設計案不進分群）
      ⚠ 這是揭露不是修正——「為什麼是 44」由 deepen §3 處理

## 8. 驗收

### 🔴 8.0 未覆蓋範圍揭露（§8.5，2026-08-19）

⚠ 「全綠」不等於「全驗過」。以下是**已知沒驗到**的，逐項寫出來而不是讓它
消失在綠燈裡——未揭露的未驗範圍，讀者會預設它驗過了。

| 未驗項 | 為什麼 | 需要什麼才能驗 |
|---|---|---|
| **成功產出的 conclusions 頁**（8.4） | 真 CLI 鏈已跑過但**尚未成功收尾**：`#426` 跑 3763 秒後死於白名單缺 `Edit`（已修並重啟 Companion） | 使用者再按一次產製簡報 |

⚠ **2026-08-20 更正：本表原有三列是錯的**，逐項說明——

| 原列 | 錯在哪 |
|---|---|
| ~~OpenSpec strict CLI 不在~~ | ⚠ **我只查了 PATH 就下結論**。CLI 一直在 `D:\vscode\openspec\`（v1.8.0）——專案規則明寫「需要某種能力時**先看 `D:\vscode\`**」，我跳過了那一步。連帶讓「CLI 已依使用者要求移除」這個記載也是錯的 |
| ~~割草機無分群主題~~ | 2026-08-20 已分群（技術 k=10、功效 k=20），阻塞消失 |
| ~~`STATUS_*` 門檻無第二批可驗~~ | 同上，已有第二批資料 |

🔴 **教訓**：「查不到」與「沒去查該查的地方」是兩件事，而寫進揭露表的樣子一模一樣。
揭露表的價值建立在「未驗項是真的驗不了」——放進一個**其實可驗**的項目，
會讓整張表的可信度打折。

**已解除的未驗項（2026-08-19）**：

| 原未驗項 | 怎麼解除 |
|---|---|
| ~~本機測試庫相關的紅~~ | 起 `pgvector/pgvector:pg18` 拋棄式容器（`127.0.0.1:5433`、trust、**不掛 volume**）＋ `alembic upgrade head`。⚠ 本機 PG18 沒有 pgvector，而 `0004` 有 `CREATE EXTENSION vector`，所以只能走 docker。實測 **16 個紅歸零**，全套從 26 分鐘（全花在連線逾時）降到 9 秒 |
| ~~兩 workspace 各產報表（滑雪機那半）~~ | 使用者授權重產，`report_trial_20260819_143341` 逐項對判準 **18/18** |
| ~~真 CLI 撰稿與目視~~ | `#426` 是**真 CLI**：真撰稿（第 1 輪用 `Write` 改了 content.json）、真目視兩輪、真產出 `visual_verdict.json`。⚠ 它**失敗**，但失敗本身就是真鏈跑通的證據——假接的 CLI 產不出那種診斷（自證東亞換行規則有效、再指出全形斜線不在禁則字集內） |

**已驗到的替代證據**：
- §8.3 以**真素材**跑 intake（`report_trial_20260811_094014`）：
  行動掃描接線修正後，證據不足 92 → 5、成立 6 → 9、每主題成立數 0–3。
  ⚠ 剩的 5 個都是 `pending_count` 缺（§7e 之後才有的欄位）——那是**資料舊**
  不是系統判不出來，正好證明兩者分開是對的。
- 半真機械鏈（真 subprocess 全鏈、真產 `.pptx`、真 Chromium）每批都跑。

⚠ 2026-08-19 修正帳目：本清單原本有**重複條目**（8.1 與 8.5 各出現兩次，
其中一份 8.5 已勾、一份沒勾），使得「1/8」這個分數同時**低報**（8.2 做完沒勾）
又**分母錯**（獨立項目是 6 項不是 8 項）。重複條目已移除。

- [x] 8.1 OpenSpec strict validation
      → **通過**（2026-08-20）：`D:\vscode\openspec\openspec.cmd` v1.8.0，
      `validate sync-report-contracts-and-palette --strict` → `is valid`。
      順手批次驗了另五個相關 change（deepen／add-deck-delivery-line／
      scope-narrative-evidence-to-workspace／add-company-group-normalization／
      add-patent-search-terms-and-db-index-governance）**六個全過**。
      ⚠ 先前標「阻塞：CLI 不在 PATH」是**我只查 PATH 沒查 `D:\vscode\`**——
      而專案規則明寫要先看那裡。見 §8.0 的更正表。
- [x] 8.2 三層範圍回歸（直接／整合／契約）
      ＋ **符號反查消費者**比對已跑清單（2026-08-18 實證：憑印象挑會漏 6 支）
      → 全套 **2637 passed / 31 failed**；31 紅逐一歸因，**無一由本 change 造成**。
      歸因方法：`merge-base origin/master` 取本分支 120 個獨有提交，篩出同時動過
      本 change 目錄的 26 個＝本 change 的提交；12 個失敗測試檔全不在其中，
      而本 change 碰過的 36 個測試檔全綠。
      ⚠ 歸因工具自己先錯一次：第一版拿 `main` 當主線（實際叫 `master`），
      merge-base 查空 → 獨有提交變空集合 → **每個 commit 都被標成「主線既有」**。
      那不是查證結果，是空集合的預設值。
      起拋棄式測試庫後 16 個環境紅歸零；剩 7 個別條線的債順手修掉 6 個
      （`test_clustering_database` 四個過期斷言的根因是**它從 0021 併表後就沒真正
      跑過**——一直紅在連線逾時上，沒人看到裡面也壞了）。
      未修 1 個：`patent_search_terms.py:97,111` 讀保存區，需 schema 決定。
- [~] 8.3 兩個 workspace 各產一份報表，逐項對 design §7 判準
      → **滑雪機完成**：`report_trial_20260819_143341` 逐項對判準 **18/18**
      （§2 封面四數字、§7e 13 個主題狀態分解、§3 家族數落點、§4 用詞、
      §9 行動掃描 covered 10/10・證據不足 0・成立數 min 0 max 4）。
      ⚠ 本項因 §10 改動報表輸出而重驗過一次——先前對
      `report_trial_20260819_122745` 驗的 18/18 已失效。
      → **割草機未完成**：該 workspace 無分群主題，產不出來（見 8.0 揭露表）。
- [~] 8.4 版型庫：`LAYOUTS` 三處同步閘門綠；`conclusions` 實際產得出來
      （⚠ 不是「閘門沒紅」——要真的看到那一頁）
      → 閘門綠；但 `#426` 死於白名單缺 `Edit`，**尚無成功產出的那一頁**。
      已修並重啟 Companion，等使用者再按一次。
      🔴 **2026-08-21 判定作廢，不再等**：`LAYOUTS` 與 `conclusions` 屬 deck 版型庫，
      而 deck 交付線已於 2026-08-20 停產、模組全數移除。實查 `backend/` 與 `skills/`
      **一處都沒有**這兩個符號，僅存於 `.agents` 筆記與
      `archive/deprecated/deck-delivery-line/`。⇒ 本項的驗收對象已不存在，
      標為不適用而非未完成。
- [x] 8.5 揭露未覆蓋範圍（見 §8.0）；⚠ 本 change **不含**有自由度的閘門
      （依據層級、行動對象、表格適用情境）——全在輪二
- [x] 8.6 使用者接受後 archive；同步 main specs 與 migration ledger
      → 2026-08-21 使用者驗收後明確指示 archive。

## 10. 報表版面實機裁決（2026-08-19 追加）

⚠ 本節是**計畫外插入**的：使用者在 §8 驗收期間實機看報表，當場裁決四項版面問題。
它們改動報表輸出，因此 **§8.3 的先前結果（對 `report_trial_20260819_122745` 驗的
18/18）已失效**，必須對改動後的版本重驗——見 10.6。

### 10.1 長條圖列高不再撐開填滿畫布

- [x] `_fill_row_height` → `_row_height`：列高由字級推導、不隨列數變動
- [x] 推翻依據不只偏好：G-7「撐開填滿 PPT 圖框」的前提是 `chart_scale()` 會二次縮放，
      而它自 2026-08-12（unify-chart-source）起**恆為 1.0**，該行為是遺留物
- [x] 實測（四個分頁全量，非抽樣）：列距 IPC 四階 54→27、IPC 五階 91→27、
      CPC 五階 108→27；畫布高 557→237；空白從條高的 4–5 倍降到 0.5 倍
- [x] 四個分頁列距一致（皆 27），切分頁時條不再跳位

### 10.2 `BAR_HEIGHT_PX` 收回單一定義處

- [x] 10.1 初版新增 `BAR_HEIGHT_PX = 18` 字面值，而檔案下方早有
      `BAR_HEIGHT_PX = _SIZING.bar_height`——同一個名字兩個定義處
- [x] ⚠ 兩邊剛好都是 18 所以**完全沒有症狀**，但改 `chart_sizing.bar_height`
      只有一半會跟著動。這正是 §6 整節在消滅的東西
- [x] 刪字面值、保留 `_SIZING` 來源；`bar_min_gap` 一併納入 `chart_sizing`

### 10.3 主題演進表轉置為「年份為列、主題為欄」

- [x] 維度：5 列 × 14 欄（須橫捲）→ 12 列 × 6 欄
- [x] ⚠ 真正的理由不是「塞得下」：年份當**鍵**時，JavaScript 的
      integer-like key 規則會把它們強制排到最前，`label`／`total` 被擠到最右
      （node 實測 `{label,"2011","2013","2026",total}` → `["2011","2013","2026","label","total"]`）。
      年份改成**值**之後這條規則就咬不到這張表
- [x] `pivot_year_matrix_by_year`；保留欄名相撞時併入而非覆蓋

### 10.4 總件數＝各主題跨年合計（末列），逐年合計退場

- [x] 轉置初版沿用 `total` 欄，語意變成「該年跨主題合計」——那是年度趨勢圖
      已經在回答的問題，在主題演進表裡既重複又佔一欄
- [x] 改為末列 `application_year = TOTAL_ROW_LABEL`；`total` 欄移除
- [x] 測試同時鎖「逐年合計不得復活」——否則兩張表會對同一個詞給出兩種數字
- [x] 驗收不只看有沒有那一列：逐主題把各年重加一次（5 個全對），
      再與主題統計表 `patent_count` 對帳（5 個全對）

### 10.5 variant 自帶 `row_count`（修掉對讀者說謊的截斷訊息）

- [x] 病徵：主題演進 12 列（完整）被標成「目前顯示 12 筆，總計 13 筆」，
      13 其實是**主題表**的列數
- [x] 成因：後端 variant 不給 `row_count`，前端三個分支都只換 `rows` 沒換 `row_count`
      ——**五個 variant 全中**
- [x] 後端補 `row_count` 並套用與 section 同一條 20 列上限；
      前端抽 `rowCountFor(rows, owner)` 一處定義，三個分支共用
- [x] ⚠ 拿不到未截斷總數時（通道過濾後的子集）回 `rows.length`＝不顯示截斷訊息，
      寧可不說也不謊報
- [x] `test_variant_rows_display` 的函式抽取清單補 `rowCountFor`
      （漏了會 ReferenceError，看起來像前端壞掉，其實是取樣清單沒跟上）

### 10.6 §8.3 重驗（本節改動報表輸出，先前結果失效）

- [x] 對 `report_trial_20260819_143341`（含 10.1–10.5）重跑 §8.3 判準
      → **18/18 全過**（§2 封面四數字、§7e 主題狀態分解、§3 家族數落點、
      §4 用詞、§9 行動掃描 covered 10/10・證據不足 0・成立數 min 0 max 4）

## 11. 全覆蓋數字驗收（2026-08-20，使用者要求「驗收所有報表數字」）

⚠ 本節也是**計畫外插入**：使用者重跑分群與報表後要求逐一驗證「數字是不是從資料
推導的」。驗收對象 `report_trial_20260820_094232`（割草機 226 件），做法是把
`report_data.json` 的**全部 221 個數值路徑、3866 個值**列出來，逐群從 DB 獨立重算
再比對——不抽樣。

- [x] 推導型門檻全數確認由本批算出：時間窗 2014–2019／2020–2024（滑雪機是 2011，
      沒被帶過來）、截止排除 2 年（末端未決比 2025 = 67%、2026 = 100%）、
      成長基準 0.7734、件數中位數 18.5／7.5、集中度門檻 39.5／41.5
- [x] 固定門檻僅剩 5 個且皆為可靠度型：`STATUS_MIN_SAMPLE`、
      `MIN_CLUSTERING_DOCUMENTS`、`STATUS_RECENT_WINDOW_YEARS`、
      `STATUS_STAGNANT_HALF_WIDTH`、`STATUS_TAIL_PENDING_RATIO`
- [x] 重算相符：逐年申請／家族／公告件數、受理局×法律狀態 12 組（合計 226）、
      IPC 全 27 類與 CPC 全 10 類、申請人排名前十、年度矩陣 54 列四欄、
      主題表 30 列件數與法律狀態四欄、象限中位數、設計保護策略（設計件數合計
      10 ＝全批設計案）、時間軸樞紐與總件數列
- [x] ⚠ 第一輪 14 項未過中 **13 項是驗證腳本自己的錯**（把 `row_count` 當成顯示
      列數、把受理局表當一維、把新增的「總件數」總計列算進總和、拿截斷後的 20 列
      去折疊）。逐一歸屬後才確認只有 1 項是真缺陷——**未過不等於報表有錯**

### 11.1 申請人口徑兩個定義處（唯一的真缺陷）

- [x] 症狀：`cluster_topic_table`／`opportunity_quadrant` 用未歸集團的
      `applicant_display_name`，其餘全報表用 `applicant_group_display_name`。
      30 個主題中 15 個受影響——技術 T001「最大一家」16% vs 35%，
      一個讀作「沒有壟斷者」、一個讀作「一家佔三分之一」，**方向相反**
- [x] 傳導範圍：`derive_thresholds` 的 max_share 中位數（功效 41.5→46.5）與
      象限 Y 軸切線（技術 9.0→8.0），故象限落點與行動建議一併改變
- [x] 修法**不是**在 loader 寫死集團欄——那只是把不一致換個方向（company scope
      會變成排名表用原始名、主題表用集團名）。改為口徑**跟隨 `report_scope`**，
      換算走 `report_engine.scoped_column`／`scoped_source_table` 唯一定義處
- [x] 來源同時從 `report_patent_base`（專利粒度）換成申請人展開 VIEW（申請人粒度），
      與排名表同一張表
- [x] ⚠ 光改 loader 不夠：`report_scope` 原本沒傳進來，預設值會讓實機維持舊行為
      而測試全綠。已接線 payload → `_resolve_report_cluster_data` →
      `_merge_cluster_channels` → `_load_report_cluster_data` → loader，
      並為「有沒有真的傳到」單獨立測試
- [x] 順帶修：`top_applicants_ws` 的 `ORDER BY cnt DESC` 無次要排序，同件數者
      順序不確定（實測第 9／10 名各 4 件會互換）。它是象限「龍頭涉入」的判定
      依據，第 10 名多家平手時誰進榜會逐次變動而報表看起來完全正常

### 11.2 排除理由沿用已被推翻的代理指標

- [x] 症狀：`cluster_topic_table:tech` 寫「10 件無分群來源文本」——**數字對、理由錯**。
      那 10 件是設計案，由 `DESIGN_DOCUMENT_KINDS` 規則排除；實測其中 1 件
      （patent_id 452）確實帶獨立項文字，先前正是靠「有沒有文本」把它漏進技術分群
- [x] 理由改為**算出來**：比對排除數與設計案件數，相符寫「排除外觀設計 N，
      不列入主題分群」，多出來的另計「另 M 件無分群來源文本」
- [x] ⚠ 判不出設計案件數時**回空字串**，不得沿用舊理由——錯的理由比沒有理由更糟：
      沒有理由讀者會去問，錯的理由讀者會直接相信
- [x] 第二落點一併修：`patent_kind.design_exclusion_note` 的「無技術請求項」
      改為「依文獻種類排除」，`kind_summary` docstring 的同一說法一併更正
- [x] 涵蓋率閘門加 `DERIVED_REASON_REPORTS` 類別——是**擴充分類**不是放寬閘門，
      那類報表仍須明列，只是理由不寫在查表裡

### 11.3 設計保護策略母體算成 0

- [x] 症狀：「母體 0/226 件」，但該區實際顯示 5 家申請人、10 件設計案
- [x] 根因：該報表一列一件專利、沒有 `patent_count` 欄，`_sum_patent_count`
      找不到就回 0
- [x] 加 `COVERED_COUNTERS` 專屬計數器（數設計案），輸出
      「母體 10/226 件（全部外觀設計案；技術案僅作同申請人交叉比對）」
- [x] ⚠ 不做成通用 fallback——「沒有件數欄就數列數」會讓日後任何忘了帶件數欄的
      報表靜默得到一個看似合理的母體

### 11.4 策略型分布的單位是「家」卻叫 `patent_count`

- [x] `design_strategy_chart_rows` 數的是申請人列數（技術+設計 4 家、只走設計 1 家），
      但欄名 `patent_count` 被 `table_display` 顯示成「專利件數」；那 5 家的設計案
      其實共 10 件
- [x] 更名為 `applicant_count`（標籤「申請人家數」已存在）。SVG 走另一份
      `matrix_rows`，改名不影響繪圖
- [x] ⚠ 數字沒錯，錯的是它自稱是什麼——這種缺陷加總與對帳全都會過

### 11.5 回歸與歸屬

- [x] TDD：11 個 Red（各自因正確原因失敗）→ 全數 Green
- [x] 改動範圍回歸 283 passed／7 skipped；`RUN_DB_TESTS=1` 真實 SQL 測試 13 passed
- [x] ⚠ 首次完整套件 20 紅。以 `git stash` 取乾淨基線逐一歸屬：**6 個是本次造成**
      （改了 `_load_report_cluster_data`／`_merge_cluster_channels` 簽章，打到 4 處
      mock；`design_exclusion_note` 措辭測試）、**1 個既有債**
      （`patent_search_terms.py:97,111` 讀 `patent_attributes`）、**13 個環境競爭**
      （拋棄式 DB 連線逾時，同批單獨跑在改動前後都全綠）
- [x] ⚠ 先前那輪的回歸範圍只挑了 15 個檔案，漏掉這 6 個消費端——真正抓到它們的
      是完整套件。`test_report_artifact_store.py:212` 早就記著同一類教訓
      （fake 簽名不跟隨真函式 → TypeError 被上層吞掉 → cluster_data 靜默變 None），
      本次在每個改到的 fake 上都補了同樣警語
- [x] 🔴 待使用者重按「產生報表」後重驗 §11 各項——現有
      `report_trial_20260820_094232` 是改動前的碼產的，主題表數字仍是舊口徑
      → **2026-08-21 重驗完成**，對象 `report_trial_20260821_114000`（割草機 226 件），
      以 Playwright 實機 ＋ 直接讀 `report_data.json` 原始值兩路比對：

      | 判準 | 修好前 | 實測 |
      |---|---|---|
      | §11.1 技術 T001 最大一家佔比 | 16% | **`max_share = 35`**（創科 15/43） |
      | §11.1 象限技術 Y 軸切線 | 9.0 | **家數中位數 8.0**（件數中位數 18.5） |
      | §11.1 象限功效 Y 軸切線 | 5.0 | **家數中位數 4.0**（件數中位數 7.5） |
      | §11.2 主題表排除說明 | 「N 件無分群來源文本」 | 含「外觀設計」，**已無**舊理由 |
      | §11.4 `design_strategy_chart_rows` 欄名 | `patent_count` | **`applicant_count`**，全庫已無 `patent_count` |

      ⚠ 未算成一項：功效通道 `max_share` 中位數（41.5→46.5）——`chart_rows` 鍵名
      與比對樣式對不上。技術通道的兩個直接判準皆命中，且與它同屬一次修正，
      據此判定通過；**此項列為未實測，不當作已驗**。
