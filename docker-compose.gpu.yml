# Optional GPU deployment overlay. Base Compose remains usable on CPU-only servers.
services:
  worker:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: ${WORKER_GPU_COUNT:-all}
              capabilities: [gpu]
